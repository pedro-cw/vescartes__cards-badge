{% comment %}
Auto-generated by Shogun-AB.
This file can be re-written at any time.
{% endcomment %}


{%- comment -%}
  =====================================================
  CONTENT TYPE DETECTION
  Assigns the appropriate content object based on template type
  =====================================================
{%- endcomment -%}
{% if article %}
  {% assign content = article %}
{% elsif blog %}
  {% assign content = blog %}
{% elsif page %}
  {% assign content = page %}
{% elsif product %}
  {% assign content = product %}
{% elsif collection %}
  {% assign content = collection %}
{% elsif metaobject %}
  {% assign content = metaobject.system %}
{% endif %}

{%- comment -%}
  =====================================================
  CONSENT TRACKING & OPTIMIZER SCRIPT
  =====================================================
{%- endcomment -%}
<script src="/cdn/shopifycloud/consent-tracking-api/v0.1/consent-tracking-api.js"></script>

<script type="text/javascript">
  {% raw %}
;(function() {
    var bt=Object.defineProperty;var yt=(v,b,I)=>b in v?bt(v,b,{enumerable:!0,configurable:!0,writable:!0,value:I}):v[b]=I;var o=(v,b,I)=>yt(v,typeof b!="symbol"?b+"":b,I);(function(){"use strict";/*! js-cookie v3.0.8 | MIT */function v(c){for(var t=1;t<arguments.length;t++){var e=arguments[t];for(var i in e)i!=="__proto__"&&(c[i]=e[i])}return c}var b={read:function(c){return c[0]==='"'&&(c=c.slice(1,-1)),c.replace(/(%[\dA-F]{2})+/gi,decodeURIComponent)},write:function(c){return encodeURIComponent(c).replace(/%(2[346BF]|3[AC-F]|40|5[BDE]|60|7[BCD])/g,decodeURIComponent)}};function I(c,t){function e(s,n,r){if(!(typeof document>"u")){r=v({},t,r),typeof r.expires=="number"&&(r.expires=new Date(Date.now()+r.expires*864e5)),r.expires&&(r.expires=r.expires.toUTCString()),s=encodeURIComponent(s).replace(/%(2[346B]|5E|60|7C)/g,decodeURIComponent).replace(/[()]/g,escape);var a="";for(var l in r)r[l]&&(a+="; "+l,r[l]!==!0&&(a+="="+r[l].split(";")[0]));return document.cookie=s+"="+c.write(n,s)+a}}function i(s){if(!(typeof document>"u"||arguments.length&&!s)){for(var n=document.cookie?document.cookie.split("; "):[],r={},a=0;a<n.length;a++){var l=n[a].split("="),d=l.slice(1).join("=");try{var u=decodeURIComponent(l[0]);if(u in r||(r[u]=c.read(d,u)),s===u)break}catch{}}return s?r[s]:r}}return Object.create({set:e,get:i,remove:function(s,n){e(s,"",v({},n,{expires:-1}))},withAttributes:function(s){return I(this.converter,v({},this.attributes,s))},withConverter:function(s){return I(v({},this.converter,s),this.attributes)}},{attributes:{value:Object.freeze(t)},converter:{value:Object.freeze(c)}})}var M=I(b,{path:"/"});const N=class N{constructor(t,e={}){o(this,"_writable");this.kind=t,this.cookieAttributes=e}get writable(){if(this._writable===void 0){const t=`${N.PROBE_KEY}_${this.kind}`;this._writable=this.set(t,"1"),this.remove(t)}return this._writable}get(t){var e;try{return this.kind==="cookie"?M.get(t)??null:((e=this.storage)==null?void 0:e.getItem(t))??null}catch(i){return console.debug(`shogun: could not read '${t}' from ${this.kind} store`,i),null}}set(t,e){try{if(this.kind==="cookie")M.set(t,e,this.cookieAttributes);else{const i=this.storage;if(!i)return!1;i.setItem(t,e)}}catch(i){return console.debug(`shogun: could not write '${t}' to ${this.kind} store`,i),!1}return this.get(t)===e}remove(t){try{if(this.kind==="cookie")M.remove(t,this.cookieAttributes);else{const e=this.storage;if(!e)return!1;e.removeItem(t)}}catch(e){return console.debug(`shogun: could not remove '${t}' from ${this.kind} store`,e),!1}return this.get(t)===null}get storage(){try{return(this.kind==="local"?globalThis.localStorage:globalThis.sessionStorage)??null}catch{return null}}};o(N,"PROBE_KEY","_shg_storage_probe");let w=N;class Q{constructor(t,e){o(this,"CACHE_KEY","_shg_audience_cache");o(this,"CACHE_EXPIRATION_DAYS",7);o(this,"DEVICE_MOBILE_BREAKPOINT",768);o(this,"cache",{timestamp:Date.now(),data:{}});o(this,"storage",new w("local"));this.visitor=t,this.locationApiUrl=e,this.restoreCache()}evaluate(t){switch(t.value=this.snakeCaseKeys(t.value),t.audience_type){case"location":return this.locationMatches(t);case"b2b":return this.b2bMatches(t);case"device":return this.deviceMatches(t);case"logged_in":return this.loggedInMatches(t);case"referrer_contains":return this.referrerContainsMatches(t);case"url_contains":return this.urlContainsMatches(t);case"new_visitor":return this.newVisitorMatches(t);case"returning_visitor":return this.returningVisitorMatches(t);default:return{status:"skipped",expected:t.value,actual:null,reason:`No evaluator implemented for audience type: ${t.audience_type}`}}}evaluateAll(t,e="every"){if(!t||t.length===0)return{status:"eligible",condition:e,expected:{},actual:{}};if(!["every","any"].includes(e))return{status:"error",condition:e,expected:{},actual:{}};const i=t.map(a=>({type:a.audience_type,evaluation:this.evaluate(a)})),s=e==="every"?i.every(a=>a.evaluation.status==="match"):i.some(a=>a.evaluation.status==="match"),n=i.some(a=>a.evaluation.status==="error"),r=e==="every"&&i.some(a=>a.evaluation.status==="no_match");return{status:s?"eligible":n&&!r?"error":"not_eligible",condition:e,expected:t.reduce((a,l)=>(a[l.audience_type]={condition:l.condition,value:l.value},a),{}),actual:i.reduce((a,l)=>(a[l.type]=l.evaluation.actual,a),{}),evaluations:i.reduce((a,l)=>(a[l.type]={status:l.evaluation.status,reason:l.evaluation.reason},a),{})}}b2bMatches(t){const e=t.value===this.visitor.isB2B;return{status:(t.condition==="is_not"?!e:e)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:this.visitor.isB2B}}}deviceMatches(t){const e=window.innerWidth<this.DEVICE_MOBILE_BREAKPOINT||window.outerWidth<this.DEVICE_MOBILE_BREAKPOINT?"mobile":"desktop",i=t.value===e;return{status:(t.condition==="is_not"?!i:i)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:e}}}truncate(t){return t?t.slice(0,200):void 0}locationMatches(t){let e=null;if(!this.cache.data.location){let h=null,m=0;try{const f=new XMLHttpRequest;f.open("GET",this.locationApiUrl,!1),f.send(null),m=f.status,m===200?h=JSON.parse(f.responseText):e={cause:"http_status",status:m,error:this.truncate(f.responseText)}}catch(f){e={cause:m===200?"invalid_response":"request_failed",status:m,error:this.truncate(String(f))}}e&&console.debug("shogun: location lookup failed, location audiences cannot be evaluated",e),h&&(this.cache.data.location={country:h==null?void 0:h.country,region:h==null?void 0:h.region,city:h==null?void 0:h.city,timestamp:Date.now()},this.saveCache())}const i={condition:t.condition,value:t.value},s=this.cache.data.location;if(!i||!i.value||!i.value.type||!i.value.country_code)return{status:"no_match",expected:i,actual:s?{value:s}:null};if(!s)return{status:"error",expected:i,actual:e,reason:"location_unavailable"};const{type:n,country_code:r,toponym_name:a,parent_name:l}=i.value;let d=!1;switch(n){case"country":d=s.country===r;break;case"region":d=s.region===a&&s.country===r;break;case"city":d=s.city===a&&s.region===l&&s.country===r;break;default:d=!1}return{status:(t.condition==="is_not"?!d:d)?"match":"no_match",expected:i,actual:{value:s}}}loggedInMatches(t){const e=String(t.value)==="true",i=this.visitor.isLoggedIn===e;return{status:(t.condition==="is_not"?!i:i)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:this.visitor.isLoggedIn}}}referrerContainsMatches(t){if(typeof t.value!="string")return{status:"error",expected:{condition:t.condition,value:t.value},actual:null,reason:"Invalid expected value"};const e=document.referrer.toLowerCase().includes(t.value.toLowerCase());return{status:(t.condition==="is_not"?!e:e)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:document.referrer}}}urlContainsMatches(t){if(typeof t.value!="string")return{status:"error",expected:{condition:t.condition,value:t.value},actual:null,reason:"Invalid expected value"};const e=window.location.href.toLowerCase().includes(t.value.toLowerCase());return{status:(t.condition==="is_not"?!e:e)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:window.location.href}}}returningVisitorMatches(t){if(!this.visitor.detailsPersisted||!this.visitor.details)return{status:"error",expected:{condition:t.condition,value:t.value},actual:null,reason:"visitor_details_unavailable"};const{expected_time_in_milliseconds:e,within_or_after:i}=t.value||{};if(typeof e!="number"||!i)return{status:"error",expected:{condition:t.condition,value:t.value},actual:null,reason:"Invalid expected data"};const s=this.visitor.details.first_visit_timestamp,n=Date.now();if(i==="within"){const r=s+e>n;return{status:(t.condition==="is_not"?!r:r)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:s}}}else{const r=s+e<n;return{status:(t.condition==="is_not"?!r:r)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:s}}}}newVisitorMatches(t){if(!this.visitor.detailsPersisted)return{status:"error",expected:{condition:t.condition,value:t.value},actual:null,reason:"visitor_details_unavailable"};const e=t.value==this.visitor.isFirstVisit;return{status:(t.condition==="is_not"?!e:e)?"match":"no_match",expected:{condition:t.condition,value:t.value},actual:{value:this.visitor.isFirstVisit}}}restoreCache(){const t=this.storage.get(this.CACHE_KEY);if(t)try{const e=JSON.parse(t);e.timestamp<=Date.now()-this.CACHE_EXPIRATION_DAYS*24*60*60*1e3?(this.storage.remove(this.CACHE_KEY),this.cache.data={}):this.cache=e}catch{this.storage.remove(this.CACHE_KEY),this.cache.data={}}}saveCache(){this.cache.timestamp=Date.now(),this.storage.set(this.CACHE_KEY,JSON.stringify(this.cache))}snakeCaseKeys(t){if(t==null||typeof t!="object")return t;const e={};for(const[i,s]of Object.entries(t)){const n=i.replace(/[A-Z]/g,r=>`_${r.toLowerCase()}`);e[n]=s}return e}}class E extends Error{constructor(e,i,s={}){super(e,{cause:s.cause});o(this,"optimization");this.reason=i,this.name="DispatchError",this.optimization=s.optimization}}const p=class p{constructor(t){o(this,"customerId");o(this,"isB2B");o(this,"details",null);o(this,"sessionId",null);o(this,"id",null);o(this,"_idsPersisted",!0);o(this,"_detailsPersisted",!0);o(this,"storage",new w("local"));o(this,"sessionCookie",new w("cookie",{expires:1/48,path:"/"}));o(this,"visitorCookie",new w("cookie",{expires:365,path:"/"}));this.context=t,this.sessionId=t.sessionId,this.customerId=t.customerId,this.isB2B=t.isB2B,this.loadCachedDetails(),this.loadAndStoreVisitorId(),this.loadAndStoreSessionId()}get bin(){return this.sessionId?this.hashWithDjb2(this.sessionId)%p.DEFAULT_NUMBER_OF_BINS:0}get firstVisitTimestamp(){var t;return((t=this.details)==null?void 0:t.first_visit_timestamp)??Date.now()}get isFirstVisit(){return this.firstVisitTimestamp>=Date.now()-p.NEW_VISITOR_THRESHOLD_MINUTES*60*1e3}get isLoggedIn(){return!!this.customerId}loadCachedDetails(){var e;const t=this.storage.get(p.DETAILS_KEY);if(t)try{this.details=JSON.parse(t),typeof((e=this.details)==null?void 0:e.first_visit_timestamp)!="number"&&(console.warn("Invalid visitor details found in storage, resetting."),this.details=null)}catch(i){console.error("Error loading visitor details from storage:",i),this.details=null}this.details||(this.details={first_visit_timestamp:Date.now()},this._detailsPersisted=this.storage.set(p.DETAILS_KEY,JSON.stringify(this.details)))}get detailsPersisted(){return this._detailsPersisted}get idsPersisted(){return this._idsPersisted}get cookiesWritable(){return this.visitorCookie.writable}newId(){try{return crypto.randomUUID()}catch{return console.debug("shogun: this platform will not generate an identifier for the visitor"),null}}loadAndStoreSessionId(){const t=this.sessionId||this.sessionCookie.get(p.SESSION_ID_KEY)||null;t?this.sessionId=t:(this.sessionId=this.newId(),this.sessionId||(this._idsPersisted=!1)),this.sessionId&&(this._idsPersisted=this.sessionCookie.set(p.SESSION_ID_KEY,this.sessionId)&&this._idsPersisted)}loadAndStoreVisitorId(){const t=this.visitorCookie.get(p.VISITOR_ID_KEY)||null;t?this.id=t:(this.id=this.newId(),this.id?this._idsPersisted=this.visitorCookie.set(p.VISITOR_ID_KEY,this.id)&&this._idsPersisted:this._idsPersisted=!1)}hashWithDjb2(t){let e=5381;for(let i=0;i<t.length;i++)e=(e<<5)-e+t.charCodeAt(i),e|=0;return Math.abs(e)}};o(p,"DETAILS_KEY","_shg_ab_visitor_details"),o(p,"SESSION_ID_KEY","_shg_ab_session_id"),o(p,"VISITOR_ID_KEY","_shg_ab_visitor_id"),o(p,"DEFAULT_NUMBER_OF_BINS",1e4),o(p,"NEW_VISITOR_THRESHOLD_MINUTES",30);let R=p;const A=class A{constructor(t){o(this,"buyItNowHandlerAttached",!1);this.config=t}run(){typeof document<"u"&&document.readyState==="loading"?document.addEventListener("DOMContentLoaded",()=>{this.attachBuyItNowHandler()}):setTimeout(()=>{this.attachBuyItNowHandler()},100)}attachBuyItNowHandler(){var s;const t=(s=this.config)==null?void 0:s.currentCartVariantId;if(!t){console.debug("shogun: no current cart variant ID assigned, buy-it-now interception skipped");return}if(this.buyItNowHandlerAttached)return;this.buyItNowHandlerAttached=!0,console.debug("shogun: setting up buy-it-now interception with cart variant ID:",t);const e=async(n,r)=>{var u,h,m,f;const a=(r==null?void 0:r.formData)??new FormData(n);let l=(r==null?void 0:r.variantId)??this.extractVariantIdFromFormData(a);if(!l){console.debug("shogun: no product variant ID found for buy-it-now, interception skipped");return}const d=(r==null?void 0:r.quantity)??(parseInt(a.get("quantity")||"1",10)||1);if(console.debug("shogun: buy-it-now details:",{productVariantId:l,quantity:d,cartVariantId:t,storefrontAccessToken:(u=this.config)!=null&&u.storefrontAccessToken?"***":null,shopDomain:(h=this.config)==null?void 0:h.shopDomain}),(m=this.config)!=null&&m.storefrontAccessToken&&((f=this.config)!=null&&f.shopDomain)){console.debug("shogun: attempting cartCreate with merchandiseId:",`gid://shopify/ProductVariant/${l}`);const x=await this.createCartAndGetCheckoutUrl(l,d,t);if(x){console.debug("shogun: redirecting to cartCreate checkoutUrl (original cart preserved)"),window.location.href=x;return}}console.debug("shogun: falling back to cart add with cart variant"),await this.addToCartAndCheckout(l,d,t)};typeof document<"u"&&(()=>{document.addEventListener("click",n=>{const r=n.target;if(!r)return;const a=this.findPaymentElementFromClick(n,r);if(!a)return;const l=this.findFormForPaymentElement(a,r,n);if(!l)return;const d=this.getBuyItNowDetailsFromForm(l);d&&(n.preventDefault(),n.stopPropagation(),n.stopImmediatePropagation(),e(l,d))},{capture:!0})})()}async createCartAndGetCheckoutUrl(t,e,i){var l,d,u,h,m,f,x;if(!((l=this.config)!=null&&l.storefrontAccessToken)||!((d=this.config)!=null&&d.shopDomain))return null;const s=`
      mutation cartCreate($input: CartInput!) {
        cartCreate(input: $input) {
          cart {
            id
            checkoutUrl
          }
          userErrors {
            field
            message
          }
        }
      }
    `,n=i?[{key:"shogun_variant_id",value:i}]:[];let r=[];i&&this.config.lineItemLevelVariant&&(r=[{key:"_shogun_variant_id",value:i}]);const a={input:{lines:[{merchandiseId:`gid://shopify/ProductVariant/${t}`,quantity:e,attributes:r}],attributes:n}};try{const L=JSON.stringify({query:s,variables:a}),Z=`https://${this.config.shopDomain}/api/2025-10/graphql.json`;console.debug("shogun: cartCreate request URL:",Z),console.debug("shogun: cartCreate request body:",L);const S=await(await fetch(Z,{method:"POST",headers:{"Content-Type":"application/json","X-Shopify-Storefront-Access-Token":this.config.storefrontAccessToken},body:L})).json();return console.debug("shogun: cartCreate full response:",JSON.stringify(S,null,2)),(m=(h=(u=S.data)==null?void 0:u.cartCreate)==null?void 0:h.cart)!=null&&m.checkoutUrl?(console.debug("shogun: cartCreate successful, checkoutUrl:",S.data.cartCreate.cart.checkoutUrl),S.data.cartCreate.cart.checkoutUrl):(console.error("shogun: cartCreate failed:",((x=(f=S.data)==null?void 0:f.cartCreate)==null?void 0:x.userErrors)||S.errors),null)}catch(L){return console.error("shogun: cartCreate error:",L),null}}async addToCartAndCheckout(t,e,i){var n;const s={items:[{id:t,quantity:e,properties:{}}]};i&&(s.attributes={shogun_variant_id:i},(n=this.config)!=null&&n.lineItemLevelVariant&&(s.items[0].properties={_shogun_variant_id:i}));try{const r=await fetch("/cart/add.js",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(s)});r.ok?(console.debug("shogun: added to cart, redirecting to checkout"),window.location.href="/checkout"):console.error("shogun: cart add failed:",await r.text())}catch(r){console.error("shogun: cart add error:",r)}}findPaymentElementFromClick(t,e){const i=e.closest(A.SHOPIFY_PAYMENT_BUTTON_SELECTOR);if(i)return i;const s=typeof t.composedPath=="function"?t.composedPath():[];for(const n of s)if(n instanceof HTMLElement&&n.matches(A.SHOPIFY_PAYMENT_BUTTON_SELECTOR))return n;return null}findFormForPaymentElement(t,e,i){const s=t.closest("form");if(s)return s;const n=e==null?void 0:e.closest("form");if(n)return n;const r=(e==null?void 0:e.closest("[form]"))||t.closest("[form]")||t.querySelector("[form]"),a=r==null?void 0:r.getAttribute("form");if(a){const h=document.getElementById(a);if(h instanceof HTMLFormElement)return h}const l=typeof(i==null?void 0:i.composedPath)=="function"?i.composedPath():[];for(const h of l)if(h instanceof HTMLFormElement)return h;const d=document.querySelectorAll("form");let u=null;for(const h of d)if(h.querySelector(A.SHOPIFY_PAYMENT_BUTTON_SELECTOR)){if(u)return console.debug("shogun: multiple payment-button forms found; skipping ambiguous buy-it-now form mapping"),null;u=h}return u}normalizeProductVariantId(t){if(!t)return null;const e=String(t);if(!e.includes("gid://"))return e;const i=e.match(/\/(\d+)$/);return i?i[1]:e}extractVariantIdFromFormData(t){const e=t.get("id")||t.get("variant_id");if(!e)return null;const i=this.normalizeProductVariantId(e);return i&&i!==e&&console.debug("shogun: extracted numeric ID from GID:",i),i}getBuyItNowDetailsFromForm(t){const e=new FormData(t),i=this.extractVariantIdFromFormData(e);if(!i)return null;const s=parseInt(e.get("quantity")||"1",10)||1;return{formData:e,variantId:i,quantity:s}}};o(A,"SHOPIFY_PAYMENT_BUTTON_SELECTOR",'shopify-buy-it-now-button, shopify-accelerated-checkout, [data-shopify="payment-button"], .shopify-payment-button');let z=A;class T{constructor(t,e,i){o(this,"id");o(this,"name");o(this,"type");o(this,"scope");o(this,"config");o(this,"variants");o(this,"assignedVariantId");o(this,"state");o(this,"eligibility");o(this,"context");o(this,"audienceEvaluator");this.id=t.id,this.name=t.name,this.type=t.type,this.scope=t.scope,this.config=t.config||{},this.variants=t.variants||[],this.assignedVariantId=t.assignedVariantId,this.state=t.assignedVariantId?t.state:void 0,this.context=e,this.audienceEvaluator=i}get isFirstAssignment(){return!!this.assignedVariantId&&this.state===void 0}get isFirstView(){return!!this.assignedVariantId&&(this.isEligible||this.isOnAssignedVariant)&&this.state!=="started"}get assignedVariant(){return this.assignedVariantId&&this.variants.find(t=>t.id===this.assignedVariantId)||null}get originalVariant(){return this.variants.find(t=>t.config.original)||null}get isOnAssignedVariant(){var t;return this.assignedVariantId?this.assignedVariantId===((t=this.currentVariant)==null?void 0:t.id):!1}get isEligible(){var t;return((t=this.eligibility)==null?void 0:t.status)==="eligible"}get firstNonDefaultEligibleVariant(){if(!this.eligibility||!this.eligibility.variants)return null;const t=Object.entries(this.eligibility.variants).filter(([i,s])=>s.status==="eligible").map(([i,s])=>i);return this.variantsByPosition().find(i=>t.includes(i.id)&&!i.config.original)||null}get inReview(){return this.type==="review"}get canDispatch(){return!!this.assignedVariant}get needsDispatch(){return this.assignedVariantId?!this.isOnAssignedVariant&&(this.inReview||this.isEligible):!1}evaluateEligibility(){const t=this.type=="ab_test"?this.evaluateAudienceEligibility():{status:"eligible",condition:void 0,expected:{},actual:{}},e=this.evaluateScopeEligibility(),i=this.evaluateVariantAudienceEligibility(),s=[t,e].every(r=>r.status==="eligible"||r.status==="match")&&(Object.keys(i).length===0||Object.values(i).some(r=>r.status==="eligible")),n=[t,e,...Object.values(i)].some(r=>r.status==="error");this.eligibility={status:s?"eligible":n?"error":"not_eligible",audience:t,scope:e,variants:i}}evaluateAudienceEligibility(){return this.audienceEvaluator.evaluateAll(this.config.audiences||[],this.config.audiences_condition||"every")}evaluateVariantAudienceEligibility(){const t={};for(const e of this.variants)e.config.audiences&&e.config.audiences.length>0?t[e.id]=this.audienceEvaluator.evaluateAll(e.config.audiences,e.config.audiences_condition||"every"):e.config.original||(t[e.id]={status:"eligible",condition:void 0,expected:{},actual:{}});return t}finalize(){this.isEligible&&this.removeViewParam()}extractPartialTemplateKey(t){if(!t)return;const e=t.split(".");return e.length<2?t:e.slice(0,-1).join(".")}variantForCurrentTemplate(){const t=this.pagePartialTemplateKey();return this.variants.find(e=>this.extractPartialTemplateKey(e.config.full_template_key)===t)||null}matchesCurrentTemplate(){return this.variantForCurrentTemplate()!==null}pagePartialTemplateKey(){const t=this.context.page.templateDirectory||(this.context.page.pageType==="metaobject"?"metaobject":null),e=t?`templates/${t}/`:"templates/",i=[this.context.page.templateName,this.context.page.templateSuffix].filter(Boolean).join(".");return e+i}extractViewParamFromFullTemplateKey(t){if(!t)return;const e=t.split("/").pop().split(".")||[];return e.slice(1,e.length-1).join(".")}variantsByPosition(){return this.variants.slice().sort((t,e)=>{const i=t.position||0,s=e.position||0;return i-s})}removeViewParam(){const t=new URL(window.location.href);t.searchParams.delete("view"),window.history.replaceState({},"",t.toString())}redirect(t){t.searchParams.delete("cache"),window.location.replace(t.toString())}}class D extends T{constructor(e,i,s){super(e,i,s);o(this,"buyItNowFinalizer",null);this.buyItNowFinalizer=new z({currentCartVariantId:this.context.page.currentCartVariantId,shopDomain:this.context.shop.shopDomain,storefrontAccessToken:this.context.shop.storefrontAccessToken})}get currentVariant(){return this.variants.find(e=>e.id===this.context.page.currentCartVariantId)||null}evaluateScopeEligibility(){return{status:"match",expected:this.scope,actual:this.scope}}dispatch(){this.dispatchCartAssignment(new URL(window.location.href))}dispatchCartAssignment(e){var n;const i=this.assignedVariant;if(!i)throw new Error(`Cannot dispatch ${this.scope} optimization because the assigned variant is not set`);const s=new XMLHttpRequest;if(s.open("POST","/cart/update.js",!1),s.setRequestHeader("Content-Type","application/json"),s.send(JSON.stringify({attributes:{shogun_variant_id:i==null?void 0:i.id}})),s.status===200){const r=(n=i==null?void 0:i.config)==null?void 0:n.theme_id;console.debug(`shogun: dispatching ${this.scope} optimization by setting cart attribute and reloading the page to reflect cart changes`+(r?` with theme_id '${r}'`:"")),this.redirect(e)}else throw new Error(`Failed to update cart for ${this.scope} optimization dispatch, status: ${s.status}`)}finalize(){var e;(this.isEligible||this.inReview)&&((e=this.buyItNowFinalizer)==null||e.run())}}class tt extends D{}class et extends T{get currentVariant(){return this.variantForCurrentTemplate()}evaluateScopeEligibility(){const{page_type:t,page_id:e}=this.config,i=!t||t===this.context.page.pageType,s=!e||e===this.context.page.pageId,n=new URLSearchParams(window.location.search).get("view");return n&&!this.variants.some(r=>this.extractViewParamFromFullTemplateKey(r.config.full_template_key)===n)?{status:"skipped",expected:void 0,actual:void 0,reason:"Visitor has view param that does not match any variant template keys, likely previewing a template change from theme editor"}:{status:this.scope==="page"&&this.matchesCurrentTemplate()&&i&&s?"match":"no_match",expected:{page_type:t,page_id:e},actual:{page_type:this.context.page.pageType,page_id:this.context.page.pageId}}}get canDispatch(){var t;return super.canDispatch&&!!this.extractViewParamFromFullTemplateKey((t=this.assignedVariant)==null?void 0:t.config.full_template_key)}dispatch(){var i,s,n;const t=new URL(window.location.href),e=this.extractViewParamFromFullTemplateKey((i=this.assignedVariant)==null?void 0:i.config.full_template_key);if(!e)throw new Error(`Assigned variant does not have a valid viewParam for dispatching full_template_key: ${(s=this.assignedVariant)==null?void 0:s.config.full_template_key}, variant: ${(n=this.assignedVariant)==null?void 0:n.id}`);t.searchParams.set("view",e),console.debug("shogun: Dispatching page optimization by redirecting to ",t.toString()),this.redirect(t)}}class it extends D{}class st extends D{}const $="_shogun_variant_id",O=`properties[${$}]`,nt=/\/cart\/add(\.js)?$/;class rt{constructor(t){o(this,"addToCartHandlerAttached",!1);o(this,"cartAddRequestInterceptionAttached",!1);this.config=t}run(){var e;const t=(e=this.config)==null?void 0:e.currentCartVariantId;if(!t){console.debug("shogun: no current cart variant ID assigned, add-to-cart interception skipped");return}this.interceptCartAddRequests(t),typeof document<"u"&&document.readyState==="loading"?document.addEventListener("DOMContentLoaded",()=>{this.attachAddToCartHandler(t)}):setTimeout(()=>{this.attachAddToCartHandler(t)},100)}attachAddToCartHandler(t){this.addToCartHandlerAttached||(this.addToCartHandlerAttached=!0,console.debug("shogun: setting up add-to-cart interception with cart variant ID:",t),document.querySelectorAll('form[action="/cart/add"]').forEach(e=>{const i=document.createElement("input");i.name=O,i.value=t,i.type="hidden",e.appendChild(i)}))}interceptCartAddRequests(t){this.cartAddRequestInterceptionAttached||(this.cartAddRequestInterceptionAttached=!0,this.interceptFetchCartAdd(t),this.interceptXhrCartAdd(t))}interceptFetchCartAdd(t){if(typeof window>"u"||typeof window.fetch!="function")return;const e=window.fetch,i=this;window.fetch=function(s,n){try{const r=((n==null?void 0:n.method)??(s instanceof Request?s.method:"GET")).toUpperCase(),a=s instanceof Request?s.url:String(s);if(r==="POST"&&i.isCartAddUrl(a)&&(n==null?void 0:n.body)!=null){const l=i.withShogunVariantProperty(n.body,t);if(l!==null)return e.call(this,s,{...n,body:l})}}catch(r){console.debug("shogun: failed to inject cart variant into fetch cart add request:",r)}return e.call(this,s,n)}}interceptXhrCartAdd(t){if(typeof XMLHttpRequest>"u")return;const e=XMLHttpRequest.prototype.open,i=XMLHttpRequest.prototype.send,s=this;XMLHttpRequest.prototype.open=function(n,r,a=!0,l,d){return this.__shogunIsCartAdd=(n==null?void 0:n.toUpperCase())==="POST"&&s.isCartAddUrl(String(r)),e.call(this,n,r,a,l,d)},XMLHttpRequest.prototype.send=function(n){if(this.__shogunIsCartAdd&&n!=null&&!(n instanceof Document))try{const r=s.withShogunVariantProperty(n,t);if(r!==null)return i.call(this,r)}catch(r){console.debug("shogun: failed to inject cart variant into XHR cart add request:",r)}return i.call(this,n)}}isCartAddUrl(t){try{const e=new URL(t,window.location.origin);return e.origin===window.location.origin&&nt.test(e.pathname)}catch{return!1}}withShogunVariantProperty(t,e){return t instanceof FormData||t instanceof URLSearchParams?(t.has(O)||t.append(O,e),t):typeof t=="string"?this.withShogunVariantPropertyInString(t,e):null}withShogunVariantPropertyInString(t,e){try{const s=JSON.parse(t);return s&&typeof s=="object"&&!Array.isArray(s)?JSON.stringify(this.withShogunVariantPropertyInPayload(s,e)):null}catch{}if(!t.includes("="))return null;const i=new URLSearchParams(t);return i.has(O)||i.append(O,e),i.toString()}withShogunVariantPropertyInPayload(t,e){return Array.isArray(t.items)?{...t,items:t.items.map(i=>i&&typeof i=="object"?{...i,properties:this.withVariantProperty(i.properties,e)}:i)}:"id"in t?{...t,properties:this.withVariantProperty(t.properties,e)}:t}withVariantProperty(t,e){const i=t&&typeof t=="object"?t:{};return i[$]!=null?i:{...i,[$]:e}}}const C=class C{constructor(){o(this,"observer",null)}removePreviewBar(t){t instanceof Element&&t.matches(C.PREVIEW_BAR_IFRAME_SELECTOR)&&(console.debug(`shogun: Removing preview bar iframe with id: ${t.id}`),t.remove(),this.stop()),t.querySelectorAll(C.PREVIEW_BAR_IFRAME_SELECTOR).forEach(e=>{console.debug(`shogun: Removing preview bar iframe with id: ${e.id}`),e.remove(),this.stop()})}run(){if(this.observer)return;const t=document.documentElement;t&&(this.observer=new MutationObserver(e=>{for(const i of e)for(const s of i.addedNodes)s instanceof Element&&this.removePreviewBar(s)}),this.observer.observe(t,{childList:!0,subtree:!0}),this.removePreviewBar(document))}stop(){var t;(t=this.observer)==null||t.disconnect(),this.observer=null}};o(C,"PREVIEW_BAR_IFRAME_SELECTOR","#preview-bar-iframe, #PBarNextFrameWrapper");let F=C;class at extends D{constructor(e,i,s){super(e,i,s);o(this,"addToCartFinalizer",null);o(this,"themePreviewFinalizer",null);this.buyItNowFinalizer=new z({currentCartVariantId:this.context.page.currentCartVariantId,shopDomain:this.context.shop.shopDomain,storefrontAccessToken:this.context.shop.storefrontAccessToken,lineItemLevelVariant:!0}),this.addToCartFinalizer=new rt({currentCartVariantId:this.context.page.currentCartVariantId})}get currentVariant(){const e=super.currentVariant;if(!e)return null;const i=e.config.theme_id;return i?i===this.context.page.themeId?e:null:e}dispatch(){var s;const e=new URL(window.location.href),i=(s=this.assignedVariant)==null?void 0:s.config.theme_id;i&&i!==this.context.page.themeId&&e.searchParams.set("preview_theme_id",i),this.dispatchCartAssignment(e)}finalize(){var e,i,s,n;(this.isEligible||this.inReview)&&((e=this.addToCartFinalizer)==null||e.run(),(i=this.buyItNowFinalizer)==null||i.run()),this.isOnAssignedVariant&&((s=this.assignedVariant)!=null&&s.config.theme_id)&&(this.themePreviewFinalizer||(this.themePreviewFinalizer=new F),(n=this.themePreviewFinalizer)==null||n.run())}}class ot extends T{get currentVariant(){return this.variantForCurrentTemplate()}evaluateScopeEligibility(){const t=this.pagePartialTemplateKey();return{status:this.matchesCurrentTemplate()?"match":"no_match",expected:t,actual:t}}get canDispatch(){var t;return super.canDispatch&&!!this.extractViewParamFromFullTemplateKey((t=this.assignedVariant)==null?void 0:t.config.full_template_key)}dispatch(){var i,s,n;const t=new URL(window.location.href),e=this.extractViewParamFromFullTemplateKey((i=this.assignedVariant)==null?void 0:i.config.full_template_key);if(!e)throw new Error(`Assigned variant does not have a valid viewParam for dispatching full_template_key: ${(s=this.assignedVariant)==null?void 0:s.config.full_template_key}, variant: ${(n=this.assignedVariant)==null?void 0:n.id}`);t.searchParams.set("view",e),console.debug("shogun: Dispatching template optimization by redirecting to ",t.toString()),this.redirect(t)}}class W extends T{constructor(e,i,s){super(e,i,s);o(this,"themePreviewFinalizer",null);this.themePreviewFinalizer=new F}get currentVariant(){return this.variants.find(e=>e.config.theme_id===this.context.page.themeId)||null}evaluateScopeEligibility(){if(!this.originalVariant)return{status:"error",expected:void 0,actual:void 0,reason:"Original variant is missing"};const e=this.originalVariant.config.theme_id;return e?{status:e===this.context.page.themeId?"match":"no_match",expected:e,actual:this.context.page.themeId}:{status:"error",expected:void 0,actual:void 0,reason:"Original variant is missing theme_id in config"}}get needsDispatch(){return this.isEligible&&!this.isOnAssignedVariant&&this.isFirstView}get canDispatch(){var e;return super.canDispatch&&!!((e=this.assignedVariant)!=null&&e.config.theme_id)}dispatch(){var s,n;const e=(s=this.assignedVariant)==null?void 0:s.config.theme_id;if(!e)throw new Error(`Assigned variant does not have a valid theme_id for dispatching, variant: ${(n=this.assignedVariant)==null?void 0:n.id}`);const i=new URL(window.location.href);i.searchParams.set("preview_theme_id",e),console.debug("shogun: Dispatching theme optimization by redirecting to ",i.toString()),this.redirect(i)}finalize(){var e;this.isOnAssignedVariant&&((e=this.themePreviewFinalizer)==null||e.run())}}o(W,"PREVIEW_BAR_IFRAME_IDS",["preview-bar-iframe","PBarNextFrameWrapper"]);class ct extends T{get currentVariant(){const t=decodeURIComponent(this.getCurrentPathWithoutLanguagePrefix());return this.variants.find(e=>e.config.path===t)||null}evaluateScopeEligibility(){const t=this.variants.find(r=>r.config.original);if(!t)return{status:"error",expected:void 0,actual:void 0,reason:"Original variant is missing"};const e=t.config.path;if(!e)return{status:"error",expected:void 0,actual:void 0,reason:"Original variant is missing path in config"};if(new URL(window.location.href).searchParams.get("view"))return{status:"skipped",expected:void 0,actual:void 0,reason:"Visitor is likely previewing a template change from theme editor"};const s=this.getCurrentPathWithoutLanguagePrefix(),n=decodeURIComponent(s);return{status:decodeURIComponent(e)===n?"match":"no_match",expected:e,actual:n}}get needsDispatch(){return!this.assignedVariantId||!this.isEligible?!1:(console.debug("shogun: Checking if URL redirect optimization needs dispatch. isOnAssignedVariant:",this.isOnAssignedVariant,"isFirstView:",this.isFirstView,"permanentRedirect:",this.config.permanent_redirect),this.config.permanent_redirect===!0&&!this.isOnAssignedVariant||!this.isOnAssignedVariant&&this.isFirstView===!0)}get canDispatch(){var t;return super.canDispatch&&!!((t=this.assignedVariant)!=null&&t.config.path)}dispatch(){var e,i,s,n,r;const t=new URL(window.location.href);if(!((e=this.assignedVariant)!=null&&e.config.path))throw new Error(`Assigned variant does not have a valid path for dispatching path: ${(i=this.assignedVariant)==null?void 0:i.config.path}, variant: ${(s=this.assignedVariant)==null?void 0:s.id}`);t.pathname=this.context.page.languageRootUrl!=="/"?this.context.page.languageRootUrl+((n=this.assignedVariant)==null?void 0:n.config.path):(r=this.assignedVariant)==null?void 0:r.config.path,console.debug("shogun: Dispatching url optimization by redirecting to ",t.toString()),this.redirect(t)}getCurrentPathWithoutLanguagePrefix(){return this.context.page.languageRootUrl==="/"||!this.context.page.path.startsWith(this.context.page.languageRootUrl)?this.context.page.path:this.context.page.path.replace(this.context.page.languageRootUrl,"")}}function G(c,t,e){switch(c.scope){case"page":return new et(c,t,e);case"template":return new ot(c,t,e);case"theme":return new W(c,t,e);case"url_redirect":return new ct(c,t,e);case"price":return new it(c,t,e);case"product_details":return new st(c,t,e);case"checkout":return new tt(c,t,e);case"shipping":return new at(c,t,e);default:return console.debug(`shogun: No implementation for optimization scope '${c.scope}', ignoring optimization '${c.name}' (${c.id})`),null}}class U{constructor(t){o(this,"visitorBin");this.visitorBin=t}getRandomVariantForVisitorBin(t){let e=null,i=0;for(const s of t)if(i+=R.DEFAULT_NUMBER_OF_BINS*this.trafficForVariant(s)/100,this.visitorBin<i){e=s;break}return e||null}trafficForVariant(t){return t.config.percentage||0}}const k=class k extends U{assign(t){var s,n;if(t.length===0)return null;const e=this.sortOptimizationsByPriority(t),i=e.find(r=>r.firstNonDefaultEligibleVariant!==null);return i?(i.assignedVariantId=(s=i.firstNonDefaultEligibleVariant)==null?void 0:s.id,i):(e[0].assignedVariantId=(n=e[0].originalVariant)==null?void 0:n.id,e[0])}sortOptimizationsByPriority(t){return t.sort((e,i)=>{const s=this.scopePriority(e.scope),n=this.scopePriority(i.scope);return s-n})}scopePriority(t){return k.SCOPE_PRIORITIES[t||"page"]??k.SCOPE_PRIORITIES.page}};o(k,"SCOPE_PRIORITIES",{theme:1,template:2,page:3,url_redirect:4,price:5,product_details:6,checkout:7,shipping:8});let B=k;class X extends U{assign(t){const e=t.filter(n=>n.isEligible);if(e.length===0)return null;const i=e[Math.floor(Math.random()*e.length)],s=this.getRandomVariantForVisitorBin(i.variants);return s?(i.assignedVariantId=s.id,i):null}}class lt extends U{assign(t){const e=t.flatMap(n=>n.variants),i=this.getRandomVariantForVisitorBin(e),s=t.find(n=>n.variants.some(r=>r.id===(i==null?void 0:i.id)))||null;return s&&i?(s.assignedVariantId=i.id,s):null}trafficForVariant(t){return t.traffic_percentage||0}}const P=class P{constructor(){o(this,"storage",new w("session"))}save(){typeof document>"u"||this.storage.set(P.STORAGE_KEY,document.referrer)||console.debug("shogun: could not retain the referrer, this visitor will be attributed to an internal redirect")}restore(){const t=this.storage.get(P.STORAGE_KEY);return t===null?typeof document<"u"&&document.referrer||null:(this.overrideDocumentReferrer(t),t||null)}clear(){this.storage.remove(P.STORAGE_KEY)}overrideDocumentReferrer(t){if(!(typeof document>"u"))try{Object.defineProperty(document,"referrer",{configurable:!0,get:()=>t})}catch(e){console.debug("shogun: failed to set original referrer via `Object.defineProperty`"),console.error(e);try{window.document.__defineGetter__("referrer",()=>t)}catch(i){console.debug("shogun: failed to set original referrer via `__defineGetter__`"),console.error(i)}}}};o(P,"STORAGE_KEY","_shg_referrer");let H=P;const g=class g{constructor(t,e){o(this,"storage",new w("session"));o(this,"previewLinkHandlerAttached",!1);o(this,"_optimizationInReview");o(this,"optimizationResolved",!1);this.context=t,this.audienceEvaluator=e}get inReview(){return!!this.optimizationInReview}get optimizationInReview(){if(!this.optimizationResolved){const t=this.previewVariantId;this._optimizationInReview=t?G({id:"review-optimization",name:"Review Optimization",type:"review",scope:this.previewScope,state:"started",variants:[{id:t,name:"Preview Variant",optimization_id:"review-optimization",traffic_percentage:100,config:{original:!1,percentage:100}}],assignedVariantId:t},{page:this.context.page,shop:this.context.shop},this.audienceEvaluator):null,this.optimizationResolved=!0}return this._optimizationInReview||null}run(){const t=document.getElementById(g.PANEL_TEMPLATE_ID);if(!t){console.debug("shogun: sidebar not found!!!");return}const e=t.content.cloneNode(!0);this.attachPreviewLinkClickHandler();const i=()=>{document.body.style.paddingLeft="360px",document.body.style.paddingTop="50px",document.body.prepend(e),typeof window.restoreCollapsedState=="function"&&window.restoreCollapsedState(),typeof window.restoreDescriptionHeightState=="function"&&window.restoreDescriptionHeightState()};if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",i);return}i()}get previewVariantId(){const e=new URLSearchParams(window.location.search).get(g.REVIEW_OPTIMIZATION_VARIANT_ID_PARAM),i=this.storage.get(g.PREVIEW_VARIANT_ID_STORAGE_KEY);return e&&e!==i?(console.debug(`shogun: theme review in progress, setting variant: ${e}`),this.storage.set(g.PREVIEW_VARIANT_ID_STORAGE_KEY,e),e):(i&&console.debug(`shogun: theme review in progress, viewing variant: ${i}`),i)}get previewScope(){const e=new URLSearchParams(window.location.search).get(g.REVIEW_OPTIMIZATION_SCOPE_PARAM),i=this.storage.get(g.PREVIEW_OPTIMIZATION_SCOPE_STORAGE_KEY);return e&&e!==i?(console.debug(`shogun: theme review in progress, setting scope: ${e}`),this.storage.set(g.PREVIEW_OPTIMIZATION_SCOPE_STORAGE_KEY,e),e):i?(console.debug(`shogun: theme review in progress, using scope: ${i}`),i):g.DEFAULT_REVIEW_SCOPE}attachPreviewLinkClickHandler(){this.previewLinkHandlerAttached||(this.previewLinkHandlerAttached=!0,document.addEventListener("click",t=>{var n,r;const e=(r=(n=t.target).closest)==null?void 0:r.call(n,'a[id^="shg-preview-btn-"]'),i=e==null?void 0:e.dataset.variantId;if(!i)return;t.preventDefault();const s=new URLSearchParams(window.location.search);s.set(g.REVIEW_OPTIMIZATION_VARIANT_ID_PARAM,i),window.location.href=`${window.location.pathname}?${s.toString()}`}))}};o(g,"PANEL_TEMPLATE_ID","shogun-price-test-preview"),o(g,"PREVIEW_VARIANT_ID_STORAGE_KEY","_shg_preview_variant_id"),o(g,"PREVIEW_OPTIMIZATION_SCOPE_STORAGE_KEY","_shg_preview_optimization_scope"),o(g,"REVIEW_OPTIMIZATION_VARIANT_ID_PARAM","shgpvid"),o(g,"REVIEW_OPTIMIZATION_SCOPE_PARAM","shgsc"),o(g,"DEFAULT_REVIEW_SCOPE","product_details");let K=g;var dt=" daum[ /]| deusu/|(?:^|[^g])news(?!sapphire)|(?<! channel/|google/)google(?!(?:wv|app|/google| pixel))|(?<! cu)bots?(?:\\b|_)|(?<!cam)scan|(?<!lib)http|24x7|;\\s\\w+;$|@[a-z][\\w-]+\\.|\\(\\)|\\.com\\b|\\b\\w+/1\\.0;|\\bbw/|\\bdlc\\b|\\bort/|\\bperl\\b|\\btime/|\\||^[<\\(;]|^[\\w \\.\\-\\(?:\\):%]+(?:/v?\\d+(?:\\.\\d+)?(?:\\.\\d{1,10})*?)?(?:,|$)|^[\\w\\-]+/[\\w]+$|^[^ ]{50,}$|^\\d+\\b|^\\w*search\\b|^\\w+/[\\w\\(\\)]*$|^\\w+/\\d\\.\\d\\s\\([\\w@]+\\)$|^active|^ad muncher|^amaya|^apache/|^avsdevicesdk/|^azure|^biglotron|^blackbox exporter|^bot|^clamav[ /]|^claude-code/|^client/|^cobweb/|^custom|^ddg[_-]android|^discourse|^dispatch/\\d|^downcast/|^duckduckgo|^email|^exodusmovement|^facebook|^getright/|^gozilla/|^hobbit|^hotzonu|^hwcdn/|^igetter/|^jeode/|^jetty/|^jigsaw|^microsoft bits|^movabletype|^mozilla/\\d\\.\\d\\s[\\w\\.-]+$|^mozilla/\\d\\.\\d\\s\\((?:compatible;)?(?:\\s?[\\w\\d-.]+\\/\\d+\\.\\d+)?\\)$|^navermailapp|^netsurf|^offline|^openai/|^owler|^php|^postman|^ps_daily/|^python|^rank|^read|^reed|^remove\\.bg/|^rest|^rss|^snapchat|^sora |^space bison|^stape/|^svn|^swcd |^taringa|^thumbor/|^track|^w3c|^webbandit/|^webcopier|^wget|^whatsapp|^wordpress|^xenu link sleuth|^yahoo|^yandex|^zdm/\\d|^zoom marketplace/|abuse|advisor|agent\\b|analyzer|archive|ask jeeves/teoma|attracta|audit|bluecoat drtr|browsex|burpcollaborator|capture|catch|check\\b|checker|chrome-lighthouse|chromeframe|classifier|cloudflare|collapsify\\b|convertify|cookiehubverify/|crawl|cursor/|cypress/|dareboost|datanyze|dejaclick|detect|discovery|dmbrowser|download|exaleadcloudview|feed|firephp|foregenix|functionize|grab|hardenize\\b|headless|hotjar|httrack|hubspot marketing grader|ibisbrowser|infrawatch|insight|inspect|iplabel|java(?!;)|library|linkcheck|linktiger|mail\\.ru/|manager|manus-user/|marketgoo/|measure|monitor\\b|neustar wpm|node\\b|nutch|offbyone|openvas|optimize|pageburst|pagespeed|parser|phantomjs|pingdom|playwright|powermarks|preview|productfinder|prospectingstudio|proxy|ptst[ /]\\d|radar|readable/|retriever|rexx;|rigor|rss\\b|scrape|securityheaders|selenium|server|silktide|sindup/|sogou|sparkler/|speedcurve|spider|splash|statuscake|supercleaner|synapse|synthetic|testlocally|tools|torrent|transcoder|url|validator|virtuoso|wappalyzer|watchtowr|webglance|webkit2png|whatcms/|xtate/",ht=/bot|crawl|http|lighthouse|scan|search|spider/i,V;function ut(){if(V instanceof RegExp)return V;try{V=new RegExp(dt,"i")}catch{V=ht}return V}var gt=c=>typeof c=="string"&&c!=="";function pt(c){return gt(c)&&ut().test(c)}var ft=pt;const y=class y{constructor(t){o(this,"storage",new w("local"));this.publishedOptimizationIds=t}get writable(){return this.storage.writable}get legacyEntries(){return Object.values(this.getState()).filter(t=>typeof t=="string").length}get(t){const e=this.getState()[t];return!e||typeof e=="string"?void 0:{...e,state:e.state??"started"}}update(t){var s,n,r,a;if(t.type!=="ab_test"||t.inReview)return!0;console.debug(`shogun: Caching assignment for optimization: '${t.name}' (${t.id}) to variant: '${(s=t.assignedVariant)==null?void 0:s.name}' (${(n=t.assignedVariant)==null?void 0:n.id})'`);const e=this.getState(),i=t.state??"assigned";return this.storage.set(y.STORAGE_KEY,JSON.stringify({...e,[t.id]:{variantId:t.assignedVariantId,themeId:(a=(r=t.assignedVariant)==null?void 0:r.config)==null?void 0:a.theme_id,state:i}}))}clearExpired(){console.debug("shogun: clearing expired optimization state, redirecting to default theme");const t=new URL(window.location.href);t.searchParams.set(y.PREVIEW_THEME_PARAM,""),this.storage.remove(y.STORAGE_KEY),window.location.replace(t.toString())}hasExpiredEntries(){const t=Object.entries(this.getState()).filter(([e,i])=>typeof i=="string"?!0:!!i.themeId).map(([e])=>e);return t.length>0&&t.some(e=>!this.publishedOptimizationIds.includes(e))}getState(){try{const t=JSON.parse(this.storage.get(y.STORAGE_KEY)||"{}");return!t||typeof t!="object"||Array.isArray(t)?{}:t}catch{return this.storage.remove(y.STORAGE_KEY),{}}}};o(y,"STORAGE_KEY","_shg_ab_optimizations_cache"),o(y,"PREVIEW_THEME_PARAM","preview_theme_id");let Y=y;function mt(){const c=wt();return{navigation_type:(c==null?void 0:c.type)??null,prerendering:"prerendering"in document?!!document.prerendering:null,activation_start:typeof(c==null?void 0:c.activationStart)=="number"?Math.round(c.activationStart):null,visibility:document.visibilityState,elapsed_ms:Math.round(performance.now()),webdriver:vt()}}function vt(){try{return"webdriver"in navigator?!!navigator.webdriver:null}catch(c){return console.debug("shogun: could not read whether this page is driven by automation",c),null}}function wt(){try{return performance.getEntriesByType("navigation")[0]??null}catch(c){return console.debug("shogun: could not read the navigation timing for this page",c),null}}class j{constructor(t){o(this,"pixelTrackingStarted",!1);o(this,"assignedOptimization",null);o(this,"pageViewId",j.newPageViewId());this.context=t}static newPageViewId(){try{return crypto.randomUUID()}catch{return null}}get storewideCollectionAllowed(){return this.assignedOptimization||this.context.publishedOptimizations.some(t=>t.type==="ab_test")?!0:!!this.context.shop.storewideEventCollection}async track(t,e,{reason:i,details:s}={}){var a,l;if(e!=null&&e.inReview)return;console.debug(`shogun: tracking event '${t}' for optimization '${(e==null?void 0:e.name)||"none"}' (${(e==null?void 0:e.id)||"none"}) ${i?`with reason '${i}'`:""}`),this.assignedOptimization=e??this.assignedOptimization,(t=="started"||t=="viewed"||t=="skipped")&&this.startPixelTracking().catch(d=>this.reportCollectionFailure(d));const n=(a=window.Shopify)==null?void 0:a.customerPrivacy;let r=!1;try{r=(n==null?void 0:n.analyticsProcessingAllowed())??!1}catch(d){console.debug("shogun: could not read the consent status",d)}try{const d=await fetch(`${this.context.shop.analyticsUrl}/dispatcher/${t}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:t,shop_id:this.context.shop.shopId,pageview_id:this.pageViewId,session_id:this.context.visitor.sessionId,visitor_id:this.context.visitor.id,optimization_id:e==null?void 0:e.id,variant_id:(l=e==null?void 0:e.assignedVariant)==null?void 0:l.id,reason:i,details:{optimization_ids:this.context.publishedOptimizations.map(u=>u.id),distribution_method:this.context.assignmentStrategy,context:this.context.page,default_theme_id:this.context.shop.defaultThemeId??null,legacy_cache_entries:this.context.legacyCacheEntries,client_timestamp:new Date().toISOString(),optimizations:this.context.publishedOptimizations.map(u=>({id:u.id,assigned_variant_id:u.assignedVariantId,first_assignment:u.isFirstAssignment,eligibility:u.eligibility})),consented:r,consent_api:n?"available":"unavailable",environment:{version:this.context.version,storage:this.context.storage,...mt()},...s}}),keepalive:!0});d.ok||console.error(`shogun: the analytics endpoint refused the '${t}' event with status ${d.status}, it was not recorded`)}catch(d){console.error("shogun: failed to send tracking event",d)}}async startPixelTracking(){var i,s,n,r,a;if(console.debug("shogun: attempting to start pixel tracking for optimizations"),this.pixelTrackingStarted)return;if(!this.storewideCollectionAllowed){console.debug("shogun: no optimization assigned and store-wide event collection is disabled, skipping pixel tracking");return}const[t,e]=await Promise.all([this.getCustomerPrivacy(),this.getAnalytics()]);t.analyticsProcessingAllowed()||(console.debug("shogun: Visitor has not consented to tracking, unable to start pixel tracking for optimizations, waiting for consent..."),await this.waitForConsent(t),this.track("consented").catch(l=>console.error("shogun: could not report consent",l))),console.debug("shogun: Visitor has consented to tracking, starting pixel tracking for optimizations");try{e.publish("shogun:load",{app_type:((i=this.assignedOptimization)==null?void 0:i.type)=="personalization"?"personalization":"ab_testing",page_type:this.context.page.pageType,page_id:this.context.page.pageId,optimization_id:((s=this.assignedOptimization)==null?void 0:s.id)??null,variant_id:((r=(n=this.assignedOptimization)==null?void 0:n.assignedVariant)==null?void 0:r.id)??null,on_assigned_variant:((a=this.assignedOptimization)==null?void 0:a.isOnAssignedVariant)??!1,session_id:this.context.visitor.sessionId,visitor_id:this.context.visitor.id,pageview_id:this.pageViewId,original_referrer:this.context.originalReferrer}),this.pixelTrackingStarted=!0}catch(l){this.reportCollectionFailure(l)}}reportCollectionFailure(t){console.error("shogun: pixel tracking did not start, this visitor will not be collected",t);const e=t instanceof E?t.reason:"analytics_unavailable";this.track("errored",this.assignedOptimization??void 0,{reason:e,details:{error:t instanceof Error?{message:t.message,stack:t.stack}:String(t)}}).catch(i=>console.error("shogun: could not report the collection failure",i))}async getShopify(){return this.until(()=>window.Shopify,"Timed out waiting for Shopify API","analytics_unavailable")}async getAnalytics(){const t=await this.getShopify();return this.until(()=>{const e=t.analytics;return e!=null&&e.publish?e:void 0},"Timed out waiting for Shopify analytics API","analytics_unavailable")}async getCustomerPrivacy(){const t=await this.getShopify();return t!=null&&t.customerPrivacy?t.customerPrivacy:(await this.loadConsentTrackingApi(),this.until(()=>{var e;return(e=window.Shopify)==null?void 0:e.customerPrivacy},"Timed out waiting for Shopify customerPrivacy API","analytics_unavailable"))}loadConsentTrackingApi(){return new Promise((t,e)=>{const i=window.Shopify;if(!(i!=null&&i.loadFeatures)){e(new E("Shopify.loadFeatures is not available","analytics_unavailable"));return}i.loadFeatures([{name:"consent-tracking-api",version:"0.1"}],s=>{s&&s.length>0?e(s[0]):t()})})}waitForConsent(t){return t.analyticsProcessingAllowed()?Promise.resolve():new Promise((e,i)=>{let s=!1,n=!1;const r=()=>{document.removeEventListener("visitorConsentCollected",a)},a=()=>{try{s=t.analyticsProcessingAllowed(),s||(n=!0)}catch(l){console.debug("shogun: could not read the consent status after it was collected",l)}};document.addEventListener("visitorConsentCollected",a),this.until(()=>s?!0:void 0,"Timed out waiting for user consent","consent_timeout").then(()=>{r(),e()}).catch(l=>{r(),i(n?new E("Consent was declined","consent_declined",{cause:l}):l)})})}until(t,e,i,{intervalMs:s=250,timeoutMs:n=15e3}={}){return new Promise((r,a)=>{const l=t();if(l){r(l);return}const d=Date.now(),u=window.setInterval(()=>{const h=t();if(h){window.clearInterval(u),r(h);return}Date.now()-d>n&&(window.clearInterval(u),a(new E(e,i)))},s)})}}const _t={location_unavailable:"location_unavailable",visitor_details_unavailable:"storage_unavailable"},J=c=>c instanceof Error?{message:c.message,stack:c.stack}:String(c),_=class _{constructor(t){o(this,"visitor");o(this,"audienceEvaluator");o(this,"retainedReferrer");o(this,"originalReferrer");o(this,"optimizationReview");o(this,"optimizationCache");o(this,"attemptsStore",new w("session"));o(this,"tracker");o(this,"legacyCacheEntries");o(this,"_publishedOptimizations");o(this,"_assignedOptimization");this.context=t,this.retainedReferrer=new H,this.originalReferrer=this.retainedReferrer.restore(),this.visitor=new R(t.session),this.optimizationCache=new Y(t.publishedOptimizations.map(e=>e.id)),this.legacyCacheEntries=this.optimizationCache.legacyEntries,this.audienceEvaluator=new Q(this.visitor,t.shop.geoLocationApi),this.optimizationReview=new K(t,this.audienceEvaluator)}get publishedOptimizations(){return this._publishedOptimizations||(this._publishedOptimizations=this.context.publishedOptimizations.flatMap(t=>{var n,r;const e=this.optimizationCache.get(t.id),i=(e==null?void 0:e.variantId)||t.assignedVariantId,s=(e==null?void 0:e.state)||t.state;if(i){const a=((r=(n=t.variants)==null?void 0:n.find(l=>l.id===i))==null?void 0:r.name)||i;console.debug(`shogun: Found cached optimization for optimization: '${t.name}' (${t.id}), with variant: '${a}' (${i})`)}return G({...t,assignedVariantId:i,state:s},{page:this.context.page,shop:this.context.shop},this.audienceEvaluator)??[]})),this._publishedOptimizations}get abTestStrategy(){switch(this.context.assignmentStrategy){case"greedy":return new X(this.visitor.bin);case"even":return new lt(this.visitor.bin);default:return new X(this.visitor.bin)}}get personalizationStrategy(){return new B(this.visitor.bin)}get assignedOptimization(){return this._assignedOptimization===void 0&&(this._assignedOptimization=this.optimizationReview.optimizationInReview||this.previouslyAssignedOptimization||this.abTestStrategy.assign(this.publishedOptimizations.filter(t=>t.type==="ab_test"))||this.personalizationStrategy.assign(this.publishedOptimizations.filter(t=>t.type==="personalization"))||null),this._assignedOptimization}get previouslyAssignedOptimization(){return this.publishedOptimizations.find(t=>t.assignedVariantId!==void 0)||null}get isBot(){return ft(navigator.userAgent)}get dispatchAttempts(){return Number(this.attemptsStore.get(_.DISPATCH_ATTEMPTS_KEY)||"0")}get trackingService(){return this.tracker||(this.tracker=new j({shop:this.context.shop,page:this.context.page,visitor:this.visitor,assignmentStrategy:this.context.assignmentStrategy,publishedOptimizations:this.publishedOptimizations,originalReferrer:this.originalReferrer,storage:{cookie:this.visitor.cookiesWritable,local:this.optimizationCache.writable,session:this.attemptsStore.writable},version:_.VERSION,legacyCacheEntries:this.legacyCacheEntries})),this.tracker}get isThemePreview(){var i,s,n;if(this.optimizationReview.inReview)return!1;const t=this.context.page.themeId!=this.context.shop.defaultThemeId,e=(n=(s=(i=this.previouslyAssignedOptimization)==null?void 0:i.assignedVariant)==null?void 0:s.config)==null?void 0:n.theme_id;return t&&(e===void 0||e!==this.context.page.themeId)}set dispatchAttempts(t){this.attemptsStore.set(_.DISPATCH_ATTEMPTS_KEY,t.toString())||console.debug("shogun: could not record the dispatch attempt, this session is not protected against a redirect loop")}get unassignedReason(){if(this.context.configExpired&&this.publishedOptimizations.length===0)return"config_expired";const e=this.publishedOptimizations.flatMap(i=>{var s,n;return[...Object.values(((s=i.eligibility)==null?void 0:s.audience.evaluations)??{}),...Object.values(((n=i.eligibility)==null?void 0:n.variants)??{}).flatMap(r=>Object.values(r.evaluations??{}))]}).find(i=>i.status==="error");return _t[(e==null?void 0:e.reason)??""]??"no_assigned_variant"}run(){var t,e,i,s,n;try{if(this.isBot){console.debug("shogun: Visitor identified as bot, skipping optimization assignment and execution."),this.trackingService.track("excluded",void 0,{reason:"bot"});return}const r=!this.optimizationCache.writable&&this.publishedOptimizations.some(l=>l.type==="ab_test");if(!this.visitor.idsPersisted||r){console.debug("shogun: Visitor cannot persist their assignment, skipping optimization assignment and execution."),this.trackingService.track("excluded",void 0,{reason:"storage_unavailable"});return}if(this.dispatchAttempts>=_.MAX_DISPATCH_ATTEMPTS)throw new E(`Maximum dispatch attempts of ${_.MAX_DISPATCH_ATTEMPTS} exceeded. Possible redirect loop detected.`,"max_dispatch_attempts");if(this.optimizationCache.hasExpiredEntries()){this.trackingService.track("excluded",void 0,{reason:"cache_expired"}),this.optimizationCache.clearExpired();return}if(this.isThemePreview){console.debug("shogun: Visitor identified as theme preview, skipping optimization assignment and execution."),this.trackingService.track("excluded",void 0,{reason:"theme_preview"});return}this.publishedOptimizations.forEach(l=>l.evaluateEligibility()),console.debug(`shogun: Running dispatcher with ${this.publishedOptimizations.length} published optimizations (${this.publishedOptimizations.map(l=>l.name).join(", ")})`);const a=this.assignedOptimization;if(a){if(!this.optimizationCache.update(a)){console.debug("shogun: Could not record the assignment, skipping optimization execution."),this.trackingService.track("excluded",void 0,{reason:"storage_unavailable"});return}if(a.isFirstAssignment&&this.trackingService.track("assigned",a),a.needsDispatch){if(!a.canDispatch)throw new E(`Optimization '${a.name}' (${a.id}) cannot dispatch variant '${(t=a.assignedVariant)==null?void 0:t.id}'`,"invalid_variant_config",{optimization:a});console.debug(`shogun: Dispatching optimization: ${a.name} (${a.id}) with variant: ${(e=a.assignedVariant)==null?void 0:e.name} (${(i=a.assignedVariant)==null?void 0:i.id}) [attempt ${this.dispatchAttempts+1}]`),this.dispatchAttempts==0&&this.retainedReferrer.save(),this.trackingService.track("dispatched",a),this.dispatchAttempts=this.dispatchAttempts+1;try{a.dispatch()}catch(l){throw new E(`Dispatch failed for optimization '${a.id}'`,"dispatch_failed",{cause:l,optimization:a})}return}console.debug(`shogun: Running optimization: '${a.name}' (${a.id}) with variant: '${(s=a.assignedVariant)==null?void 0:s.name}' (${(n=a.assignedVariant)==null?void 0:n.id})`),a.inReview?(console.debug("shogun: Optimization is in review, running optimization review flow for optimization"),this.optimizationReview.run()):a.isFirstView?(console.debug("shogun: This is the first view for this optimization, tracking started event."),a.state="started",this.optimizationCache.update(a),this.trackingService.track("started",a)):a.isOnAssignedVariant?(console.debug("shogun: This is not the first view for this optimization, tracking viewed event."),this.trackingService.track("viewed",a)):(console.debug("shogun: Visitor is not eligible for the assigned optimization, skipping view tracking and finalization."),this.trackingService.track("skipped",a,{reason:"not_eligible"})),this.dispatchAttempts=0,a.finalize()}else console.debug("shogun: No optimization assigned to this visitor."),this.trackingService.track("skipped",void 0,{reason:this.unassignedReason});this.retainedReferrer.clear()}catch(r){console.error("shogun: Error running optimizations",r);try{const a=r instanceof E?r:void 0;this.trackingService.track("errored",a==null?void 0:a.optimization,{reason:a==null?void 0:a.reason,details:{error:J(r),cause:(a==null?void 0:a.cause)===void 0?void 0:J(a.cause)}})}catch(a){console.error("shogun: Failed to report dispatcher error",a)}this.retainedReferrer.clear()}}};o(_,"VERSION","2.1.0"),o(_,"MAX_DISPATCH_ATTEMPTS",5),o(_,"DISPATCH_ATTEMPTS_KEY","_shg_dispatch_attempts");let q=_;window.ShogunDispatcher=q})();

})();
  {% endraw %}
</script>

{%- comment -%}
  =====================================================
  SHOP CONFIGURATION
  Set up namespaces, variant IDs, and payment detection
  =====================================================
{%- endcomment -%}
{%- assign shogun_app_namespace = 'app--214825828353' -%}
{%- assign shogun_variant_id = cart.attributes.shogun_variant_id -%}

{%- comment -%} Detect Klarna payment support {%- endcomment -%}
{%- assign optimizer_supports_klarna = false -%}
{%- if shop.enabled_payment_types != blank -%}
  {%- for payment_type in shop.enabled_payment_types -%}
    {%- assign payment_type_downcase = payment_type | downcase -%}
    {%- if payment_type_downcase contains 'klarna' -%}
      {%- assign optimizer_supports_klarna = true -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}
{%- endif -%}
{%- if optimizer_supports_klarna == false and shop.enabled_payment_gateways != blank -%}
  {%- for payment_gateway in shop.enabled_payment_gateways -%}
    {%- assign gateway_name = payment_gateway.name | default: payment_gateway -%}
    {%- assign gateway_value = gateway_name | downcase -%}
    {%- if gateway_value contains 'klarna' -%}
      {%- assign optimizer_supports_klarna = true -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}
{%- endif -%}

{%- comment -%}
  =====================================================
  PRICE TEST CONFIGURATION
  Only rendered when shogun_variant_id is present
  =====================================================
{%- endcomment -%}
{%- if shogun_variant_id != nil -%}
  {%- assign price_tests = shop.metafields.app--214825828353.price-tests -%}
  {%- assign optimization_config = nil -%}
  {% for optimization in price_tests.value %}
    {% for variants in optimization.variants %}
      {% if variants.id == shogun_variant_id %}
        {% assign optimization_config = optimization %}
      {% endif %}
    {% endfor %}
  {% endfor %}

  {%- assign shg_is_product_details = false -%}
  {%- if optimization_config.scope == 'product_details' -%}
    {%- assign shg_is_product_details = true -%}
  {%- endif -%}

  {%- comment -%}
    =====================================================
    SVG ICON DEFINITIONS
    =====================================================
  {%- endcomment -%}
  {%- capture variant_arrow_symbol -%}
    <svg class="shg-preview__variant-arrow" viewBox="0 0 10 12" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M4.92296 10.5227C4.55973 10.8482 4.55973 11.3758 4.92296 11.7012C5.28619 12.0267 5.8751 12.0267 6.23833 11.7012L9.33869 8.92346C9.70192 8.59802 9.70192 8.07038 9.33869 7.74494L6.23833 4.96717C5.8751 4.64173 5.28619 4.64173 4.92296 4.96717C4.55973 5.2926 4.55973 5.82024 4.92296 6.14568L6.43552 7.50087L4.65054 7.50087C3.10948 7.50087 1.86021 6.38158 1.86021 5.00087L1.86021 0.834201C1.86021 0.373964 1.44379 0.000867717 0.930106 0.000867762C0.416423 0.000867807 -4.79388e-07 0.373964 -4.39152e-07 0.834201L-6.07103e-07 5.00087C-4.05927e-07 7.30205 2.08212 9.16753 4.65054 9.16753L6.43552 9.16753L4.92296 10.5227Z" fill="currentColor"/>
    </svg>
  {%- endcapture -%}

  {%- capture eye_symbol -%}
    <svg class="shg-preview__product-eye" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
      <path d="M10 4.5C6.5 4.5 3.5 7 2 10C3.5 13 6.5 15.5 10 15.5C13.5 15.5 16.5 13 18 10C16.5 7 13.5 4.5 10 4.5Z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
      <circle cx="10" cy="10" r="2.5" stroke="currentColor" stroke-width="1.5" fill="none"/>
    </svg>
  {%- endcapture -%}

  {%- capture info_icon_symbol -%}
    <svg xmlns="http://www.w3.org/2000/svg" width="28" height="28" fill="none">
      <rect width="28" height="28" fill="#E0F0FF" rx="8"/>
      <path fill="#00527C" d="M14 18a.75.75 0 0 1-.75-.75v-3.5a.75.75 0 0 1 1.5 0v3.5A.75.75 0 0 1 14 18ZM13 11a1 1 0 1 1 2 0 1 1 0 0 1-2 0Z"/>
      <path fill="#00527C" fill-rule="evenodd" d="M21 14a7 7 0 1 1-14 0 7 7 0 0 1 14 0Zm-1.5 0a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0Z" clip-rule="evenodd"/>
    </svg>
  {%- endcapture -%}

{%- capture caret_symbol -%}
  <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 20 20" fill="none"><path fill-rule="evenodd" clip-rule="evenodd" d="M6.23966 11.7996C6.5432 12.0814 7.01775 12.0639 7.2996 11.7603L10 8.85221L12.7004 11.7603C12.9823 12.0639 13.4568 12.0814 13.7603 11.7996C14.0639 11.5177 14.0815 11.0432 13.7996 10.7397L10.5496 7.23966C10.4077 7.08683 10.2086 7 10 7C9.79145 7 9.59232 7.08683 9.45041 7.23966L6.20041 10.7397C5.91856 11.0432 5.93613 11.5177 6.23966 11.7996Z" fill="#4A4A4A"/></svg>
{%- endcapture -%}

{%- capture shogun_logo_symbol -%}
  <svg width="22" height="8" viewBox="0 0 22 8" fill="none" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd" d="M5.90331 5.45477H6.58525C6.64547 5.45477 6.67256 5.38691 6.65224 5.31296C6.64971 5.28631 6.64689 5.25936 6.64399 5.23166C6.63221 5.11908 6.61912 4.99403 6.61912 4.82577V3.24147C6.61912 2.39518 6.07116 1.95527 5.38922 1.95527C4.86761 1.95527 4.51384 2.22592 4.27976 2.52403V0.479986C4.27976 0.270321 4.1789 0.175781 3.97868 0.175781H3.35696C3.28319 0.175781 3.25007 0.223051 3.25007 0.290906C3.25007 0.343196 3.26002 0.394353 3.27225 0.457244L3.27225 0.457248L3.27225 0.45725C3.29175 0.557584 3.31706 0.687781 3.31706 0.900079V5.3404C3.31706 5.38081 3.32986 5.41436 3.35696 5.43494C3.3705 5.44867 3.39007 5.45553 3.41717 5.45553H4.17212C4.1789 5.45553 4.19244 5.45553 4.19922 5.44867C4.24664 5.44181 4.27976 5.4014 4.27976 5.3404V3.26891C4.42653 3.00435 4.68019 2.80841 5.02116 2.80841C5.42836 2.80841 5.65568 3.07907 5.65568 3.46485V5.18411C5.65568 5.33964 5.74299 5.45477 5.90331 5.45477ZM8.6895 1.46919C7.91573 1.46919 7.34294 1.36321 7.30756 1.35635C7.09982 1.3167 6.96283 1.1139 7.00121 0.903473C7.04035 0.693046 7.23982 0.554286 7.44831 0.593169L7.44853 0.593209C7.47655 0.598282 8.66152 0.812842 9.8637 0.592407C10.0722 0.554286 10.2709 0.694571 10.3085 0.904998C10.3462 1.11619 10.2077 1.31747 9.99994 1.35559C9.54004 1.44098 9.08843 1.46919 8.6895 1.46919ZM0.49195 4.52004C0.538617 4.44532 0.598832 4.43846 0.665822 4.47277C0.793027 4.54062 1.21378 4.73656 1.62776 4.73656C1.90174 4.73656 2.05529 4.61458 2.05529 4.46591C2.05529 4.25201 1.82686 4.18319 1.38958 4.05147L1.37335 4.04658C0.71851 3.84377 0.310551 3.53881 0.303777 2.98376C0.303777 2.40128 0.73808 1.95526 1.61346 1.95526C2.14787 1.95526 2.47529 2.13138 2.75605 2.30064C2.82981 2.34105 2.84938 2.39518 2.82304 2.44931L2.52874 2.92277C2.48884 2.98376 2.42863 2.99063 2.36841 2.96318C2.20131 2.86178 1.89422 2.74665 1.59314 2.74665C1.24539 2.74665 1.16561 2.90219 1.16561 3.01045C1.16561 3.20639 1.41927 3.28111 1.73389 3.36955C2.54228 3.59294 2.96981 3.88418 2.96981 4.46591C2.96981 5.16962 2.35486 5.50813 1.60669 5.50813C0.924748 5.50813 0.443778 5.22375 0.263132 5.10176C0.223239 5.06822 0.20969 5.02018 0.236787 4.95995L0.49195 4.52004ZM17.5077 2.11714V5.34598C17.5077 5.40697 17.4678 5.45424 17.4076 5.45424H16.6527C16.5857 5.45424 16.5458 5.41384 16.5458 5.34598V4.93275C16.3117 5.23086 15.9647 5.50151 15.4363 5.50151C14.7544 5.50151 14.2064 5.0616 14.2064 4.21532V2.63101C14.2064 2.42135 14.1861 2.27877 14.1665 2.14383C14.153 2.06911 14.1801 2.00202 14.2403 2.00202H14.9155C15.0826 2.00202 15.1691 2.11714 15.1691 2.27267V3.99193C15.1691 4.37771 15.3964 4.64837 15.8036 4.64837C16.1446 4.64837 16.3915 4.45243 16.545 4.18787V2.11638C16.545 2.05538 16.5917 2.00125 16.6519 2.00125H17.4001C17.4678 2.00202 17.5077 2.04929 17.5077 2.11714ZM20.5593 5.454H21.2413C21.2947 5.454 21.3218 5.38614 21.3083 5.31295C21.2887 5.178 21.2751 5.03543 21.2751 4.82577V3.24146C21.2751 2.39518 20.7204 1.95526 20.0452 1.95526C19.5168 1.95526 19.1631 2.22592 18.929 2.52403V2.1108C18.929 2.04294 18.8959 2.00253 18.8289 2.00253H18.0672C18.0137 2.00253 17.9671 2.0498 17.9671 2.1108V5.33964C17.9671 5.40749 18.0069 5.45476 18.0739 5.45476H18.8296C18.8899 5.45476 18.9297 5.40063 18.9297 5.33964V3.26815C19.0833 3.00359 19.337 2.80765 19.6712 2.80765C20.0851 2.80765 20.3124 3.0783 20.3124 3.46409V5.18334C20.3124 5.33887 20.399 5.454 20.5593 5.454ZM10.6598 6.0505C10.705 6.00552 10.7719 5.98874 10.8329 6.00628L11.2213 6.11531C11.3018 6.13818 11.353 6.2129 11.353 6.29752V6.29905C11.353 6.65129 11.7407 6.75955 12.2555 6.75955C12.6364 6.75955 13.0037 6.63146 13.0037 6.34022C13.0037 6.21137 12.9367 6.08938 12.6627 6.04211L12.2322 5.96816H12.2307L12.1494 5.95444C11.7617 5.88658 11.3335 5.79433 11.1076 5.74477C11.0414 5.728 10.9782 5.70894 10.9187 5.68606C10.6372 5.57856 10.4453 5.40092 10.4453 5.10129C10.4453 4.70864 10.7787 4.46543 11.1332 4.50584C10.9857 4.36327 10.6854 4.05906 10.6718 3.48344C10.6583 2.62343 11.2936 1.96013 12.269 1.96013C12.4303 1.96013 12.5673 1.97734 12.7149 1.99587C12.9711 2.02804 13.2589 2.06417 13.7594 2.02112C13.8196 2.01426 13.873 2.06153 13.8663 2.12938L13.8392 2.53575C13.7729 2.60285 13.572 2.62343 13.365 2.63029C13.6593 2.85368 13.8128 3.19829 13.8128 3.60466C13.8128 4.46467 13.1249 5.02657 12.2758 5.01971C11.9967 5.01971 11.8089 4.94809 11.6547 4.88931C11.6317 4.88056 11.6095 4.87209 11.5879 4.86418C11.3207 4.79022 11.2273 4.89162 11.2273 4.99989C11.2273 5.15152 11.4305 5.18141 11.7378 5.22662L11.7617 5.23014L12.7839 5.37881C13.5795 5.5008 14.0206 5.72419 14.0206 6.34022C14.0206 7.24064 13.1316 7.57915 12.1561 7.56543C11.2205 7.55856 10.4588 7.22005 10.4588 6.55675C10.4588 6.33565 10.5394 6.17173 10.6598 6.0505ZM12.9313 3.49146C12.9313 3.0584 12.6242 2.78088 12.2366 2.78088C11.8625 2.78088 11.5411 3.04468 11.5411 3.47773C11.5411 3.89706 11.8414 4.18831 12.2494 4.18831C12.6641 4.18831 12.9313 3.89096 12.9313 3.49146ZM6.84718 3.70923C6.84718 2.64184 7.64578 1.92136 8.65891 1.92136C9.66526 1.92136 10.4639 2.64184 10.4639 3.70923C10.4639 4.76975 9.66526 5.49024 8.65891 5.49024C7.64578 5.48948 6.84718 4.76899 6.84718 3.70923ZM9.57823 3.70755C9.57823 3.15708 9.19586 2.74919 8.65919 2.74919C8.11575 2.74919 7.73338 3.15708 7.73338 3.70755C7.73338 4.25801 8.11575 4.65905 8.65919 4.65905C9.19586 4.65905 9.57823 4.25801 9.57823 3.70755Z" fill="currentColor"/></svg>
{%- endcapture -%}


  {%- comment -%}
    =====================================================
    PRODUCT PAGE: PRICE AND PRODUCT DETAILS TEST PRICING DATA
    =====================================================
  {%- endcomment -%}
  {% if product %}
    {%- assign product_pricing_metafield = product.metafields[shogun_app_namespace]['product-pricing'] -%}
    {%- assign product_pricing_array = nil -%}
    {% if product_pricing_metafield and product_pricing_metafield.value %}
      {% assign product_pricing_array = product_pricing_metafield.value %}
    {% endif %}

    {%- comment -%} Primary payload: per-variant metafields + product-level default {%- endcomment -%}
    <script type="application/json" id="shg-price-test-pricing">
      {
        "shogunVariantId": {{ shogun_variant_id | json }},
        "variants": {
          {% assign first_pricing_entry = true %}
          {% for variant in product.variants %}
            {% assign shogun_variant_pricing = variant.metafields[shogun_app_namespace]['product-variant-pricing'] %}
            {% if shogun_variant_pricing and shogun_variant_pricing.value %}
              {% assign matched_pricing = shogun_variant_pricing.value | find: 'shogun_variant_id', shogun_variant_id %}
              {% if matched_pricing %}
                {% unless first_pricing_entry %},{% endunless %}
                "{{ variant.id }}": {{ matched_pricing | json }}
                {% assign first_pricing_entry = false %}
              {% endif %}
            {% endif %}
          {% endfor %}
        },
        "productPricing": {% if product_pricing_array %}{{ product_pricing_array | json }}{% else %}null{% endif %}
      }
    </script>


    {%- comment -%} Fallback payload: build from optimization_config (covers draft preview & missing metafields) {%- endcomment -%}
    {% assign current_product_config = nil %}
    {% if optimization_config %}
      {% for p in optimization_config.products %}
        {% if p.id == product.id %}
          {% assign current_product_config = p %}
        {% endif %}
      {% endfor %}
    {% endif %}

    {% if current_product_config %}
      <script type="application/json" id="shg-price-test-pricing-fallback">
        {
          "shogunVariantId": {{ shogun_variant_id | json }},
          "variantPricing": {
            {% for vp in current_product_config.variant_pricing %}
              "{{ vp.id }}": { "test_price": {{ vp.test_price | json }}, "original_price": {{ vp.original_price | json }} }{% unless forloop.last %},{% endunless %}
            {% endfor %}
          }
        }
      </script>
    {% endif %}
  {% endif %}

  {%- comment -%} Include Klarna support script if enabled {%- endcomment -%}
  {% if optimizer_supports_klarna %}
    <script>
(function () {
  if (window.__shgKlarnaLite) return;

  // ---------- tiny helpers ----------
  function isArray(v){ return Array.isArray ? Array.isArray(v) : Object.prototype.toString.call(v)==='[object Array]'; }
  function findEntry(list, pred){ if(!isArray(list)) return null; for(var i=0;i<list.length;i++) if(pred(list[i])) return list[i]; return null; }
  function toCents(x){ var n = Math.round(parseFloat(x) * 100); return (isFinite(n) && n >= 0) ? n : null; }

  // ---------- read test amount from your JSON blobs ----------
  var dataNode = document.getElementById('shg-price-test-pricing');
  var defaultCents = null;     // chosen product price (your test price)
  var priceMap = {};           // per-variant overrides if provided

  if (dataNode) {
    try {
      var payload = JSON.parse(dataNode.textContent || dataNode.innerHTML || '{}');

      // 1) Main product price (chosen/test price for this shogunVariantId)
      if (payload && payload.shogunVariantId && isArray(payload.productPricing)) {
        var chosen = findEntry(payload.productPricing, function (e){ return e.shogun_variant_id === payload.shogunVariantId; })
                 || findEntry(payload.productPricing, function (e){ return e.original; });
        if (chosen && chosen.price != null) defaultCents = toCents(chosen.price);
      }

      // 2) Optional per-variant map (if present)
      var variants = (payload && payload.variants) || {};
      Object.keys(variants).forEach(function(vid){
        var entries = variants[vid];
        if (!isArray(entries)) return;
        var match = findEntry(entries, function (e){return e.shogun_variant_id === payload.shogunVariantId;})
                 || findEntry(entries, function (e){return e.original;});
        if (match && match.price != null) {
          var cents = toCents(match.price);
          if (cents != null) priceMap[String(vid)] = cents;
        }
      });
    } catch(_){}
  }

  // Fallback blob (optional)
  if (!Object.keys(priceMap).length && defaultCents == null) {
    var fb = document.getElementById('shg-price-test-pricing-fallback');
    if (fb) {
      try {
        var fbPayload = JSON.parse(fb.textContent || '{}');
        if (fbPayload && fbPayload.variantPricing) {
          Object.keys(fbPayload.variantPricing).forEach(function(id){
            var vp = fbPayload.variantPricing[id];
            var chosen = (vp && (vp.test_price != null ? vp.test_price : vp.original_price));
            var c = toCents(chosen);
            if (c != null) priceMap[String(id)] = c;
          });
        }
      } catch(_){}
    }
  }

  // Final amount resolver:
  function resolveOverrideCents(){
    if (typeof defaultCents === 'number') return defaultCents;
    var ids = Object.keys(priceMap);
    if (ids.length) return priceMap[ids[0]];
    return null;
  }

  // ---------- (A) keep DOM placements in sync ----------
  function resyncDom(){
    var amt = resolveOverrideCents();
    if (amt == null) return;

    // Set both DOM attributes (safe on DOM)
    var nodes = document.querySelectorAll('klarna-placement,[data-klarna-osm]');
    for (var i=0;i<nodes.length;i++){
      nodes[i].setAttribute('data-purchase-amount', String(amt));
      nodes[i].setAttribute('data-payment-amount',  String(amt));
    }

    // Nudge OSM to re-render with the enforced amount
    try {
      if (window.KlarnaOnsiteService && typeof window.KlarnaOnsiteService.push === 'function') {
        window.KlarnaOnsiteService.push({ event: 'update', purchaseAmount: amt, paymentAmount: amt });
      }
    } catch(_){}
  }

  // Guard direct attribute writes so Klarna can't revert them
  (function patchSetAttribute(){
    var proto = Element.prototype;
    var orig = proto.setAttribute;
    if (!orig || proto.__shgSetAttrPatched) return;
    proto.setAttribute = function(name, value){
      try {
        var n = String(name || '').toLowerCase();
        if ((this && this.nodeType === 1) && (n === 'data-purchase-amount' || n === 'purchase-amount' || n === 'data-payment-amount' || n === 'payment-amount')) {
          var amt = resolveOverrideCents();
          return orig.call(this, name, String(amt != null ? amt : value));
        }
      } catch(_){}
      return orig.call(this, name, value);
    };
    proto.__shgSetAttrPatched = true;
  })();

  // Snap back any attribute changes (e.g., after modal closes)
  (function installObserver(){
    try {
      var obs = new MutationObserver(function(records){
        var amt = resolveOverrideCents();
        if (amt == null) return;
        for (var i=0;i<records.length;i++){
          var r = records[i];
          if (r.type !== 'attributes' || !r.target) continue;
          var name = (r.attributeName || '').toLowerCase();
          if (name && (name === 'data-purchase-amount' || name === 'purchase-amount' || name === 'data-payment-amount' || name === 'payment-amount')) {
            var cur = r.target.getAttribute(r.attributeName);
            var want = String(amt);
            if (cur !== want) r.target.setAttribute(r.attributeName, want);
          }
        }
      });
      obs.observe(document.documentElement, {
        subtree: true,
        attributes: true,
        attributeFilter: ['data-purchase-amount','purchase-amount','data-payment-amount','payment-amount']
      });
    } catch(_){}
  })();

  // ---------- (B) force amount in Klarna network calls (no duplicates) ----------
  function whichAmountKey(url){
    // Prefer the key already present; otherwise infer from placement_key
    if (url.searchParams.has('purchase_amount')) return 'purchase_amount';
    if (url.searchParams.has('payment_amount'))  return 'payment_amount';

    var pk = url.searchParams.get('placement_key') || '';
    // Known defaults:
    if (/payment-calculator-interstitial/i.test(pk)) return 'purchase_amount';
    if (/credit-promotion-badge/i.test(pk))         return 'payment_amount';

    // Fallback: default to purchase_amount
    return 'purchase_amount';
  }

  function forceAmountInUrl(u){
    try{
      var url = (u instanceof Request) ? new URL(u.url, location.href) : new URL(String(u), location.href);
      var isKlarnaMsg = /js\.klarna\.com.*\/(cma|eu\/cma)\/v\d+\/messaging/i.test(url.href) || /klarna.*\/messaging/i.test(url.href);
      if (!isKlarnaMsg) return null;

      var amt = resolveOverrideCents();
      if (amt == null) return null;

      // Decide exactly ONE param to set
      var key = whichAmountKey(url);
      url.searchParams.set(key, String(amt));

      // And IMPORTANT: remove the other key if present to avoid "duplicated" error
      var other = key === 'purchase_amount' ? 'payment_amount' : 'purchase_amount';
      url.searchParams.delete(other);

      return url.toString();
    }catch(_){ return null; }
  }

  // fetch() patch
  (function patchFetch(){
    try {
      var _fetch = window.fetch;
      if (typeof _fetch === 'function' && !_fetch.__shgPatched) {
        window.fetch = function(input, init){
          try{
            var forced = forceAmountInUrl(input);
            if (forced) {
              if (input instanceof Request) {
                input = new Request(forced, input);
              } else {
                input = forced;
              }
            }
          }catch(_){}
          return _fetch.call(this, input, init);
        };
        window.fetch.__shgPatched = true;
      }
    } catch(_){}
  })();

  // XHR patch
  (function patchXHR(){
    try {
      var _open = XMLHttpRequest.prototype.open;
      var _send = XMLHttpRequest.prototype.send;

      if (!_open.__shgPatched) {
        XMLHttpRequest.prototype.open = function(method, url, async, user, password){
          try{ this.__shgForcedUrl = forceAmountInUrl(url) || null; }catch(_){}
          this._lastMethod = method;
          return _open.apply(this, arguments);
        };
        _open.__shgPatched = true;
      }

      if (!_send.__shgPatched) {
        XMLHttpRequest.prototype.send = function(body){
          try{
            if (this.__shgForcedUrl) {
              _open.call(this, this._lastMethod || 'GET', this.__shgForcedUrl, true);
            }
          }catch(_){}
          return _send.apply(this, arguments);
        };
        _send.__shgPatched = true;
      }
    } catch(_){}
  })();

  // ---------- (C) kick + keep things fresh around modal lifecycle ----------
  function kick(){
    resyncDom();
  }

  // Initial + a short burst to catch late mounts
  kick();
  var end = Date.now() + 6000;
  (function loop(){
    kick();
    if (Date.now() < end) requestAnimationFrame(loop);
  })();

  // When focus returns / tab visible (often right after modal closes)
  document.addEventListener('visibilitychange', function(){ if (!document.hidden) kick(); }, { passive: true });
  window.addEventListener('focus', kick, { passive: true });

  // Variant selectors often fire "change"
  document.addEventListener('change', function(e){
    if ((e.target && /variant|size|color|option/i.test(String(e.target.name||''))) || e.isTrusted) kick();
  }, { passive: true });

  window.__shgKlarnaLite = true;
})();
</script>

  {% endif %}

  {%- comment -%}
    =====================================================
    DRAFT PREVIEW SIDEBAR
    Only rendered when optimization is in draft status
    =====================================================
  {%- endcomment -%}

  {% if optimization_config and optimization_config.status == "draft" %}
    {%- comment -%} Resolve original (A) and second (B) variant ids for sidebar so A and B show different data. {%- endcomment -%}
    {% assign shg_preview_original_variant = optimization_config.variants | where: 'original', true | first %}
    {% assign shg_preview_original_variant_id = shg_preview_original_variant.id %}
    {% assign shg_preview_second_variant = optimization_config.variants | reject: 'original', true | first %}
    {% assign shg_preview_second_variant_id = shg_preview_second_variant.id %}
    {% assign shg_description_iframe_sandbox = 'allow-same-origin allow-scripts allow-forms allow-popups allow-popups-to-escape-sandbox allow-presentation' %}

    <template id="shogun-price-test-preview">
        {%- comment -%}
        =====================================================
        PREVIEW TOP BAR - Original / Variant switcher
        =====================================================
      {%- endcomment -%}
      <div class="shg-preview__top-bar" id="shg-preview-top-bar">
        <div class="shg-preview__top-bar-logo" aria-hidden="true">
          <span class="shg-preview__logo-svg" aria-hidden="true">{{ shogun_logo_symbol }}</span>
        </div>
        <div class="shg-preview__top-bar-center">
          <span class="shg-preview__top-bar-label">Preview theme:</span>
            <a href="{{ request.path }}?shgpvid={{ shg_preview_original_variant_id }}&shgsc={{ optimization_config.scope }}" class="shg-preview__top-bar-segment shg-preview__top-bar-segment-original{% if shogun_variant_id == shg_preview_original_variant_id %} is-active{% endif %}" id="shg-preview-btn-original" data-variant-id="{{ shg_preview_original_variant_id }}">
              <span class="shg-preview__top-bar-badge shg-preview__top-bar-badge-a">A</span>
              <span class="shg-preview__top-bar-segment-label">Original details</span>
            </a>
            {% if shg_preview_second_variant_id %}
              <a href="{{ request.path }}?shgpvid={{ shg_preview_second_variant_id }}&shgsc={{ optimization_config.scope }}" class="shg-preview__top-bar-segment shg-preview__top-bar-segment-variant{% if shogun_variant_id == shg_preview_second_variant_id %} is-active{% endif %}" id="shg-preview-btn-variant" data-variant-id="{{ shg_preview_second_variant_id }}">
                <span class="shg-preview__top-bar-badge shg-preview__top-bar-badge-b">B</span>
                <span class="shg-preview__top-bar-segment-label">Variant details</span>
              </a>
            {% endif %}
        </div>
      </div>
      <div class="shg-preview__top-bar-spacer" aria-hidden="true"></div>

      {%- comment -%}
        =====================================================
        PREVIEW SIDEBAR STYLES
        =====================================================
      {%- endcomment -%}
      <style>
        /* ===== Top Bar (Original / Variant switcher) ===== */
        .shg-preview__top-bar {
          position: fixed;
          top: 0;
          left: 0;
          right: 0;
          height: 50px;
          display: flex;
          align-items: center;
          padding: 0 16px;
          gap: 24px;
          background: #fafafa;
          border-bottom: 1px solid #e5e7eb;
          font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
          z-index: 10000;
          box-sizing: border-box;
        }
        .shg-preview__top-bar-logo {
          display: flex;
          align-items: center;
          flex-shrink: 0;
        }
        .shg-preview__logo-svg {
          width: 70px;
          height: 26px;
          color: #1a1a1a;
        }
        .shg-preview__logo-svg svg {
          width: 100%;
          height: 100%;
          display: block;
        }
        .shg-preview__top-bar-center {
          display: flex;
          align-items: center;
          gap: 12px;
          flex: 1;
          justify-content: center;
        }
        .shg-preview__top-bar-label {
          font-size: 12px;
          color: #54595e;
          font-weight: 400;
          flex-shrink: 0;
        }
        .shg-preview__top-bar-segment {
          display: inline-flex;
          align-items: center;
          gap: 8px;
          padding: 6px 14px;
          font-size: 12px;
          color: #54595e;
          text-decoration: none;
          cursor: pointer;
          transition: background 0.15s ease;
          border: none;
          border-radius: 8px;
        }
        .shg-preview__top-bar-segment:hover,
        .shg-preview__top-bar-segment.is-active {
          background: #ebebeb;
        }
        .shg-preview__top-bar-badge {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 22px;
          height: 22px;
          border-radius: 50%;
          font-size: 12px;
          font-weight: 600;
          color: #fff;
          flex-shrink: 0;
        }
        .shg-preview__top-bar-badge-a { background: #343434; }
        .shg-preview__top-bar-badge-b { background: #007bff; }
        .shg-preview__top-bar-segment-label { white-space: nowrap; }
        .shg-preview__top-bar-spacer {
          width: 100%;
          height: 48px;
          flex-shrink: 0;
        }

        /* ===== Layout ===== */
        .shg-preview__sidebar {
          position: fixed;
          top: 0;
          left: 0;
          width: 360px;
          height: 100%;
          overflow-y: auto;
          background: #fff;
          border-right: 1px solid #e5e7eb;
          font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
          z-index: 9999;
          box-sizing: border-box;
          padding-bottom: 70px; /* Add padding to account for the bottom preview bar */
          padding-top: 50px;
        }

        /* ===== Header ===== */
        .shg-preview__header {
          margin-bottom: 12px;
          background: #F2F8FF;
          padding: 12px;
        }
        .shg-preview__header h3 {
          margin: 0 0 8px;
          font-size: 15px;
          font-weight: 600;
          color: #303030;
        }
        .shg-preview__header p {
          margin: 0 0 4px;
          font-size: 13px;
          color: #303030;
          line-height: 20px;
        }

        /* ===== Content Area ===== */
        .shg-preview__content {
          padding: 0 12px;
        }
        .shg-preview__currency {
          margin-top: 12px;
          font-size: 11px;
          color: #616161;
          font-weight: 500;
        }

        /* ===== Support Banner ===== */
        .shg-preview__support-banner {
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          gap: 4px;
          padding: 12px 14px;
          border: 1px solid #e5e7eb;
          border-radius: 10px;
          margin-bottom: 20px;
          background: #fff;
          box-shadow:
            1px 0 0 0 rgba(0, 0, 0, 0.13) inset,
            -1px 0 0 0 rgba(0, 0, 0, 0.13) inset,
            0 -1px 0 0 rgba(0, 0, 0, 0.17) inset,
            0 1px 0 0 rgba(204, 204, 204, 0.50) inset,
            0 1px 0 0 rgba(26, 26, 26, 0.07);
        }
        .shg-preview__support-content {
          display: flex;
          align-items: center;
          gap: 12px;
        }
        .shg-preview__support-text {
          font-size: 13px;
          color: #374151;
          margin: 0 0 8px;
        }
        .shg-preview__support-btn {
          display: inline-block;
          padding: 6px 12px;
          font-size: 12px;
          font-weight: 550;
          color: #1a1a1a;
          background: #fff;
          border: 1px solid #d1d5db;
          border-radius: 6px;
          cursor: pointer;
          text-decoration: none;
          margin-left: 40px;
        }
        .shg-preview__support-btn:hover {
          background: #f9fafb;
        }

        /* ===== Product Card ===== */
        .shg-preview__product {
          overflow: hidden;
          display: flex;
          flex-direction: column;
          gap: 4px;
          margin-bottom: 12px;
        }
        .shg-preview__product-header {
          display: flex;
          align-items: center;
          gap: 10px;
        }
        .shg-preview__product-image {
          width: 36px;
          height: 36px;
          border-radius: 6px;
          border: 1px solid #e5e7eb;
          object-fit: cover;
          flex-shrink: 0;
        }
        .shg-preview__product-title {
          font-size: 13px;
          font-weight: 550;
          color: #303030;
        }
        .shg-preview__product-content {
          padding: 8px;
          border: 1px solid #e5e7eb;
          border-radius: 8px;
        }
        .shg-preview__product-link {
          display: flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
          color: #6b7280;
          text-decoration: none;
        }
        .shg-preview__product-link:hover {
          color: #374151;
        }
        .shg-preview__product-caret {
          margin-left: auto;
          background: none;
          border: none;
          cursor: pointer;
          display: flex;
          align-items: center;
          justify-content: center;
          flex-shrink: 0;
          width: 20px;
          height: 20px;
          padding: 0;
          transition: transform 0.2s ease;
          transform: rotate(180deg); /* Point down when expanded */
        }
        .shg-preview__product-caret.collapsed {
          transform: rotate(270deg); /* Point left when collapsed */
        }
        .shg-preview__product-content.collapsed {
          display: none;
        }
        .shg-preview__product-eye {
          width: 18px;
          height: 18px;
        }
        .shg-preview__product-variant-title {
          font-size: 12px;
          font-weight: 450;
          color: #616161;
        }

        /* ===== Variant Group (Two-Column Layout) ===== */
        .shg-preview__variant-group {
          display: grid;
          grid-template-columns: 22px 1fr;
          gap: 10px;
          margin-bottom: 12px;
        }
        .shg-preview__variant-group:last-child {
          margin-bottom: 0;
        }
        .shg-preview__variant-header {
          display: contents;
        }
        .shg-preview__variant-badge {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          width: 22px;
          height: 22px;
          font-size: 11px;
          font-weight: 600;
          color: #fff;
          background: #303030;
          border-radius: 50%;
        }
        .shg-preview__variant-name {
          font-size: 13px;
          font-weight: 500;
          color: #1a1a1a;
        }
        .shg-preview__variant-content {
          display: flex;
          flex-direction: column;
          gap: 4px;
        }

        /* ===== Variant Images ===== */
        .shg-preview__variant-images {
          display: flex;
          flex-wrap: wrap;
          gap: 6px;
        }
        .shg-preview__variant-images img {
          width: 44px;
          height: 44px;
          border-radius: 6px;
          border: 1px solid #e5e7eb;
          object-fit: cover;
        }
        .shg-preview__variant-image-placeholder {
          width: 44px;
          height: 44px;
          border-radius: 6px;
          border: 1px solid #e5e7eb;
          background: #e5e7eb;
          flex-shrink: 0;
        }
        .shg-preview__variant-description {
          margin-top: 6px;
          border-radius: 6px;
          border: 1px solid #CCCCCC;
          min-height: 80px;
          height: 80px;
          overflow: hidden;
          resize: vertical;
        }
        .shg-preview__variant-description-iframe {
          display: block;
          width: 100%;
          height: 100%;
          border: 0;
          background: #f9fafb;
        }

        /* ===== Variant Options ===== */
        .shg-preview__variant-options {
          padding-top: 4px;
          display: flex;
          flex-direction: column;
          gap: 4px;
        }
        .shg-preview__variant-option {
          display: flex;
          align-items: center;
          gap: 8px;
        }
        .shg-preview__variant-arrow {
          width: 12px;
          height: 12px;
          color: #9ca3af;
          flex-shrink: 0;
        }
        .shg-preview__variant-option-image {
          width: 32px;
          height: 32px;
          border-radius: 4px;
          border: 1px solid #e5e7eb;
          object-fit: cover;
          flex-shrink: 0;
        }
        .shg-preview__variant-option-image-placeholder {
          width: 32px;
          height: 32px;
          border-radius: 4px;
          border: 1px solid #e5e7eb;
          background: #e5e7eb;
          flex-shrink: 0;
        }
        .shg-preview__variant-option-text {
          font-size: 12px;
          color: #4a4a4a;
        }
        .shg-preview__variant-option-price {
          font-size: 12px;
          color: #1a1a1a;
        }
        .shg-preview__variant-option-order-title {
          font-size: 13px;
          font-weight: 450;
          color: #303030;
          margin-top: 2px;
        }
        .shg-preview__variant-option-order {
          display: flex;
          flex-direction: column;
          gap: 8px;
        }
        .shg-preview__variant-option-order-row {
          display: flex;
          align-items: flex-start;
          gap: 8px;
        }
        .shg-preview__variant-option-order-label {
          font-size: 16px;
          font-weight: 450;
          color: #303030;
          line-height: 28px;
          flex-shrink: 0;
          min-width: 34px;
        }
        .shg-preview__variant-option-order-chips {
          display: flex;
          flex-wrap: wrap;
          gap: 8px;
        }
        .shg-preview__variant-option-order-chip {
          display: inline-flex;
          align-items: center;
          justify-content: center;
          border-radius: 12px;
          background: #E3E3E3;
          padding: 2px 10px;
          font-size: 12px;
          font-weight: 450;
          line-height: 24px;
          color: #303030;
        }
        .shg-preview__variant-option-order-title--disabled,
        .shg-preview__variant-option-order-label--disabled,
        .shg-preview__variant-option-order-chip--disabled {
          opacity: 0.4;
        }

        /* ===== Price Display ===== */
        .shg-preview__compare-at-price {
          text-decoration: line-through;
          color: #1a1a1a;
          margin-left: 4px;
        }

        /* ===== Utility Classes ===== */
        .shg-preview__text-gray {
          color: #B5B5B5 !important;
        }
      </style>

      {%- comment -%}
        =====================================================
        PREVIEW SIDEBAR HTML
        =====================================================
      {%- endcomment -%}
      <div class="shg-preview__sidebar">
        {%- comment -%} Header Section {%- endcomment -%}
        <div class="shg-preview__header">
          {% if shg_is_product_details %}
            <div id="product_details_testing">
              <h3>Review your product detail updates</h3>
              <p>We've created a variant theme with your new product details. Check any place product details appear (product pages, collection pages, featured sections).</p>
            </div>
          {% else %}
            <div id="product_pricing_testing">
              <h3>Review your price updates</h3>
              <p>We've created a variant theme with your new pricing. Check any place prices appear (product pages, collection pages, featured sections).</p>
            </div>
          {% endif %}
          <p>When finished, close this tab to return to Shogun.</p>
          <div class="shg-preview__currency">
            Prices shown in {{ cart.currency.iso_code }}
          </div>
        </div>

        {%- comment -%} Content Section {%- endcomment -%}
        <div class="shg-preview__content">
          {%- comment -%} Support Banner {%- endcomment -%}
          <div class="shg-preview__support-banner">
            <div class="shg-preview__support-content">
              {{ info_icon_symbol }}
              <p class="shg-preview__support-text">If something is wrong, please let us know.</p>
            </div>
            <button class="shg-preview__support-btn" onclick="contactSupport()">Contact support</button>
          </div>

          {%- comment -%}
            =====================================================
            PRODUCT LIST - Iterates through optimization products

            For each product:
            1. Loads product data and A/B pricing metafields
            2. Loops through variants A and B, rendering each with:
               - Conditional styling (A = gray/dimmed, B = normal/blue)
               - Single variant view OR multiple variant list
            Uses shg_preview_original_variant_id and shg_preview_second_variant_id from top of draft block.
            =====================================================
          {%- endcomment -%}
          {% assign shg_original_variant_id = shg_preview_original_variant_id %}
          {% assign shg_second_variant_id = shg_preview_second_variant_id %}

          {% for product_config in optimization_config.products %}
            {% assign shg_preview_product = all_products[product_config.handle] %}
            {% assign product_pricing_metafield = shg_preview_product.metafields[shogun_app_namespace]['product-pricing'] %}

            {% if shg_preview_product and shg_preview_product.variants.size > 0 and product_pricing_metafield.value %}
              {%- comment -%} Variant A = optimization's original (control); Variant B = second variant (first non-original) {%- endcomment -%}
              {% assign variant_a_data = product_pricing_metafield.value | find: 'shogun_variant_id', shg_original_variant_id %}
              {% assign variant_b_data = product_pricing_metafield.value | find: 'shogun_variant_id', shg_second_variant_id %}
              {% assign shg_variant_a_description = variant_a_data.description | default: shg_preview_product.description %}
              {% assign shg_variant_b_description = variant_b_data.description | default: shg_preview_product.description %}
              {% assign shg_variant_a_description_html = shg_variant_a_description | downcase %}
              {% assign shg_variant_b_description_html = shg_variant_b_description | downcase %}
              {% capture shg_variant_a_description_srcdoc %}<!doctype html><html lang='en'><head><meta charset='utf-8' /><meta name='viewport' content='width=device-width, initial-scale=1' /><style>body{margin:0;padding:8px 10px;font-size:12px;line-height:1.35;color:#B5B5B5;overflow-wrap:anywhere;font-family:system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;background:#f9fafb}p{margin:0 0 8px 0}p:last-child{margin-bottom:0}ul,ol{margin:8px 0;padding-left:18px}ul{list-style:disc}ol{list-style:decimal}table{width:100%;border-collapse:collapse;table-layout:fixed;margin:8px 0}th,td{border:1px solid #d1d5db;padding:4px 8px;vertical-align:top;text-align:left;word-break:break-word}th{font-weight:600}a{text-decoration:underline;text-underline-offset:2px;word-break:break-word}img,video{max-width:100%;height:auto}table img,table video{display:block;max-width:100%;height:auto}</style></head><body>{{ shg_variant_a_description }}</body></html>{% endcapture %}
              {% capture shg_variant_b_description_srcdoc %}<!doctype html><html lang='en'><head><meta charset='utf-8' /><meta name='viewport' content='width=device-width, initial-scale=1' /><style>body{margin:0;padding:8px 10px;font-size:12px;line-height:1.35;color:#1a1a1a;overflow-wrap:anywhere;font-family:system-ui,-apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,'Helvetica Neue',Arial,sans-serif;background:#f9fafb}p{margin:0 0 8px 0}p:last-child{margin-bottom:0}ul,ol{margin:8px 0;padding-left:18px}ul{list-style:disc}ol{list-style:decimal}table{width:100%;border-collapse:collapse;table-layout:fixed;margin:8px 0}th,td{border:1px solid #d1d5db;padding:4px 8px;vertical-align:top;text-align:left;word-break:break-word}th{font-weight:600}a{text-decoration:underline;text-underline-offset:2px;word-break:break-word}img,video{max-width:100%;height:auto}table img,table video{display:block;max-width:100%;height:auto}</style></head><body>{{ shg_variant_b_description }}</body></html>{% endcapture %}
              {% assign shg_variant_a_description_text = shg_variant_a_description | strip_html | replace: '&nbsp;', '' | replace: '&#160;', '' | strip %}
              {% assign shg_variant_b_description_text = shg_variant_b_description | strip_html | replace: '&nbsp;', '' | replace: '&#160;', '' | strip %}
              {% assign shg_variant_a_has_non_text_content = false %}
              {% assign shg_variant_b_has_non_text_content = false %}
              {% if shg_variant_a_description_html contains '<img' or shg_variant_a_description_html contains '<video' or shg_variant_a_description_html contains '<table' or shg_variant_a_description_html contains '<ul' or shg_variant_a_description_html contains '<ol' or shg_variant_a_description_html contains '<blockquote' %}
                {% assign shg_variant_a_has_non_text_content = true %}
              {% endif %}
              {% if shg_variant_b_description_html contains '<img' or shg_variant_b_description_html contains '<video' or shg_variant_b_description_html contains '<table' or shg_variant_b_description_html contains '<ul' or shg_variant_b_description_html contains '<ol' or shg_variant_b_description_html contains '<blockquote' %}
                {% assign shg_variant_b_has_non_text_content = true %}
              {% endif %}
              {% assign shg_variant_a_has_description = false %}
              {% assign shg_variant_b_has_description = false %}
              {% if shg_variant_a_description_text != blank or shg_variant_a_has_non_text_content %}
                {% assign shg_variant_a_has_description = true %}
              {% endif %}
              {% if shg_variant_b_description_text != blank or shg_variant_b_has_non_text_content %}
                {% assign shg_variant_b_has_description = true %}
              {% endif %}

              <div class="shg-preview__product">
                <div class="shg-preview__product-header">
                  <img class="shg-preview__product-image" src="{{ shg_preview_product.featured_image | image_url: width: 64 }}" alt="{{ shg_preview_product.title }}" />
                  <span class="shg-preview__product-title">{{ shg_preview_product.title }}</span>
                  <a href="{{ shg_preview_product.url }}?shgpvid={{ shogun_variant_id }}&shgsc={{ optimization_config.scope }}" class="shg-preview__product-link" title="View product page">
                    {{ eye_symbol }}
                  </a>
                  <button class="shg-preview__product-caret" onclick="toggleProduct('shg-preview-{{ optimization_config.id }}-product-{{ shg_preview_product.handle }}')">
                    {{ caret_symbol }}
                  </button>
                </div>

                <div class="shg-preview__product-content" id="shg-preview-{{ optimization_config.id }}-product-{{ shg_preview_product.handle }}">

                  {%- comment -%} Calculate currency conversion ratio once per product using first variant {%- endcomment -%}
                  {%- assign shg_first_variant = shg_preview_product.variants.first -%}
                  {%- assign shg_first_variant_pricing = shg_first_variant.metafields[shogun_app_namespace]['product-variant-pricing'] -%}
                  {%- assign shg_first_original_pricing = shg_first_variant_pricing.value | find: 'original', true -%}
                  {%- assign shg_presentment_price = shg_first_variant.price -%}
                  {%- assign shg_shop_currency_price_cents = shg_first_original_pricing.price | times: 100 -%}
                  {%- if shg_presentment_price != shg_shop_currency_price_cents and shg_shop_currency_price_cents != 0 -%}
                    {%- assign shg_currency_ratio = shg_presentment_price | divided_by: shg_shop_currency_price_cents -%}
                  {%- else -%}
                    {%- assign shg_currency_ratio = 1 -%}
                  {%- endif -%}

                  {% if shg_preview_product.variants.size == 1 %}
                    {%- comment -%}
                     ==================================
                     ===== SINGLE VARIANT PRODUCT =====
                     Variant A = optimization original; Variant B = second variant
                     ==================================
                    {%- endcomment -%}

                    {% if variant_a_data %}
                    {%- comment -%} Variant A (Original) - from variant_a_data (optimization's original) {%- endcomment -%}
                    {%- assign shg_converted_price = variant_a_data.price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                    {%- assign shg_converted_compare_at = variant_a_data.compare_at_price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                    <div class="shg-preview__variant-group">
                      <div class="shg-preview__variant-header">
                        <span class="shg-preview__variant-badge">A</span>
                      </div>
                      <div class="shg-preview__variant-content">
                        {% if shg_is_product_details %}
                          <span class="shg-preview__variant-name shg-preview__text-gray">
                            {{ variant_a_data.title | default: shg_preview_product.title }}
                          </span>
                        {% endif %}
                        <div class="shg-preview__variant-option">
                          <span class="shg-preview__variant-option-price shg-preview__text-gray">
                            {{ shg_converted_price | money }}
                            {% if variant_a_data.compare_at_price != blank and variant_a_data.compare_at_price != "" and variant_a_data.compare_at_price != "0" and variant_a_data.compare_at_price != "0.00" %}
                              <span class="shg-preview__compare-at-price shg-preview__text-gray">({{ shg_converted_compare_at | money }})</span>
                            {% endif %}
                          </span>
                        </div>
                        {% if shg_is_product_details %}
                          <div class="shg-preview__variant-images">
                            {% if variant_a_data.media and variant_a_data.media.size > 0 %}
                              {% for media_item in variant_a_data.media %}
                                <img src="{{ media_item.preview_url }}" alt="{{ media_item.alt | default: variant_a_data.title | default: shg_preview_product.title }}" />
                              {% endfor %}
                            {% else %}
                              <img src="{{ shg_preview_product.featured_image | image_url: width: 80 }}" alt="{{ shg_preview_product.title }}" />
                            {% endif %}
                          </div>
                          {% if shg_variant_a_has_description %}
                            <div class="shg-preview__variant-description shg-preview__text-gray" data-height-key="opt-{{ optimization_config.id }}-product-{{ shg_preview_product.id }}-variant-a">
                              <iframe
                                title="{{ shg_preview_product.title | escape }} variant A description"
                                class="shg-preview__variant-description-iframe"
                                sandbox="{{ shg_description_iframe_sandbox }}"
                                allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                                srcdoc="{{ shg_variant_a_description_srcdoc | escape }}"
                              ></iframe>
                            </div>
                          {% endif %}
                        {% endif %}
                      </div>
                    </div>
                    {% endif %}

                    {%- comment -%} Variant B (Test) - from variant_b_data (second variant) {%- endcomment -%}
                    {% if variant_b_data %}
                      {%- assign shg_converted_price = variant_b_data.price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                      {%- assign shg_converted_compare_at = variant_b_data.compare_at_price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                      <div class="shg-preview__variant-group">
                        <div class="shg-preview__variant-header">
                          <span class="shg-preview__variant-badge" style="background: #0094D5;">B</span>
                        </div>
                        <div class="shg-preview__variant-content">
                          {% if shg_is_product_details %}
                            <span class="shg-preview__variant-name">
                              {{ variant_b_data.title | default: shg_preview_product.title }}
                            </span>
                          {% endif %}
                          <div class="shg-preview__variant-option">
                            <span class="shg-preview__variant-option-price">
                              {{ shg_converted_price | money }}
                              {% if variant_b_data.compare_at_price != blank and variant_b_data.compare_at_price != "" and variant_b_data.compare_at_price != "0" and variant_b_data.compare_at_price != "0.00" %}
                                <span class="shg-preview__compare-at-price">({{ shg_converted_compare_at | money }})</span>
                              {% endif %}
                            </span>
                          </div>
                          {% if shg_is_product_details %}
                            <div class="shg-preview__variant-images">
                              {% if variant_b_data.media and variant_b_data.media.size > 0 %}
                                {% for media_item in variant_b_data.media %}
                                  <img src="{{ media_item.preview_url }}" alt="{{ media_item.alt | default: variant_b_data.title | default: shg_preview_product.title }}" />
                                {% endfor %}
                              {% else %}
                                <span class="shg-preview__variant-image-placeholder" aria-hidden="true"></span>
                              {% endif %}
                            </div>
                            {% if shg_variant_b_has_description %}
                              <div class="shg-preview__variant-description"  data-height-key="opt-{{ optimization_config.id }}-product-{{ shg_preview_product.id }}-variant-b">
                                <iframe
                                  title="{{ shg_preview_product.title | escape }} variant B description"
                                  class="shg-preview__variant-description-iframe"
                                  sandbox="{{ shg_description_iframe_sandbox }}"
                                  allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                                  srcdoc="{{ shg_variant_b_description_srcdoc | escape }}"
                                ></iframe>
                              </div>
                            {% endif %}
                          {% endif %}
                        </div>
                      </div>
                    {% endif %}

                  {% else %}

                    {%- comment -%}
                     ====================================
                     ===== MULTIPLE VARIANT PRODUCT =====
                     ====================================
                    {%- endcomment -%}

                    {% if variant_a_data %}
                    {%- comment -%} Variant A (Original) - header from variant_a_data; each row from variant metafield by shg_original_variant_id {%- endcomment -%}
                    <div class="shg-preview__variant-group">
                      <div class="shg-preview__variant-header">
                        <span class="shg-preview__variant-badge">A</span>
                      </div>
                      <div class="shg-preview__variant-content">
                        {% if shg_is_product_details %}
                          <span class="shg-preview__variant-name shg-preview__text-gray">
                            {{ variant_a_data.title | default: shg_preview_product.title }}
                          </span>
                          <div class="shg-preview__variant-images">
                            {% if variant_a_data.media and variant_a_data.media.size > 0 %}
                              {% for media_item in variant_a_data.media %}
                                <img src="{{ media_item.preview_url }}" alt="{{ media_item.alt | default: variant_a_data.title | default: shg_preview_product.title }}" />
                              {% endfor %}
                            {% else %}
                              <img src="{{ shg_preview_product.featured_image | image_url: width: 80 }}" alt="{{ shg_preview_product.title }}" />
                            {% endif %}
                          </div>
                          {% if shg_variant_a_has_description %}
                            <div class="shg-preview__variant-description shg-preview__text-gray" data-height-key="opt-{{ optimization_config.id }}-product-{{ shg_preview_product.id }}-variant-a">
                              <iframe
                                title="{{ shg_preview_product.title | escape }} variant A description"
                                class="shg-preview__variant-description-iframe"
                                sandbox="{{ shg_description_iframe_sandbox }}"
                                allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                                srcdoc="{{ shg_variant_a_description_srcdoc | escape }}"
                              ></iframe>
                            </div>
                          {% endif %}
                        {% endif %}
                        <div class="shg-preview__product-variant-title shg-preview__text-gray">Product variants:</div>
                        <div class="shg-preview__variant-options">
                          {% for variant in shg_preview_product.variants %}
                            {% assign variant_original = variant.metafields[shogun_app_namespace]['product-variant-pricing'].value | find: 'shogun_variant_id', shg_original_variant_id %}
                            {% if variant_original %}
                              {%- assign shg_converted_price = variant_original.price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                              {%- assign shg_converted_compare_at = variant_original.compare_at_price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                              <div class="shg-preview__variant-option">
                                {{ variant_arrow_symbol }}
                                {% if shg_is_product_details %}
                                  {% if variant_original.media and variant_original.media.preview_url %}
                                    <img class="shg-preview__variant-option-image" src="{{ variant_original.media.preview_url }}" alt="{{ variant_original.media.alt | default: variant.title }}" />
                                  {% else %}
                                    {% assign variant_a_fallback_media = variant_a_data.media | first %}
                                    {% if variant_a_fallback_media and variant_a_fallback_media.preview_url %}
                                      <img class="shg-preview__variant-option-image" src="{{ variant_a_fallback_media.preview_url }}" alt="{{ variant_a_fallback_media.alt | default: variant.title }}" />
                                    {% else %}
                                      <span class="shg-preview__variant-option-image-placeholder" aria-hidden="true"></span>
                                    {% endif %}
                                  {% endif %}
                                {% endif %}
                                <span class="shg-preview__variant-option-text shg-preview__text-gray">{{ variant.title | default: "Default" }}:</span>
                                <span class="shg-preview__variant-option-price shg-preview__text-gray">
                                  {{ shg_converted_price | money }}
                                  {% if variant_original.compare_at_price != blank and variant_original.compare_at_price != "" and variant_original.compare_at_price != "0" and variant_original.compare_at_price != "0.00" %}
                                    <span class="shg-preview__compare-at-price shg-preview__text-gray">({{ shg_converted_compare_at | money }})</span>
                                  {% endif %}
                                </span>
                              </div>
                            {% endif %}
                          {% endfor %}
                        </div>
                        {% if shg_is_product_details and variant_a_data.product_options and variant_a_data.product_options.size > 0 %}
                          <div class="shg-preview__variant-option-order-title shg-preview__variant-option-order-title--disabled">Variant option order:</div>
                          <div class="shg-preview__variant-option-order">
                            {% for option in variant_a_data.product_options %}
                              {% if option and option.values and option.values.size > 0 %}
                                <div class="shg-preview__variant-option-order-row">
                                  <span class="shg-preview__variant-option-order-label shg-preview__variant-option-order-label--disabled">{{ option.name | default: 'Option' }}</span>
                                  <div class="shg-preview__variant-option-order-chips">
                                    {% for option_value in option.values %}
                                      <span class="shg-preview__variant-option-order-chip shg-preview__variant-option-order-chip--disabled">{{ option_value.name | default: 'Value' }}</span>
                                    {% endfor %}
                                  </div>
                                </div>
                              {% endif %}
                            {% endfor %}
                          </div>
                        {% endif %}
                      </div>
                    </div>
                    {% endif %}

                    {%- comment -%} Variant B (Test) - header from variant_b_data; each row from variant metafield by second variant id {%- endcomment -%}
                    {% if variant_b_data %}
                      <div class="shg-preview__variant-group">
                        <div class="shg-preview__variant-header">
                          <span class="shg-preview__variant-badge" style="background: #0094D5;">B</span>
                        </div>
                        <div class="shg-preview__variant-content">
                          {% if shg_is_product_details %}
                            <span class="shg-preview__variant-name">
                              {{ variant_b_data.title | default: shg_preview_product.title }}
                            </span>
                            <div class="shg-preview__variant-images">
                              {% if variant_b_data.media and variant_b_data.media.size > 0 %}
                                {% for media_item in variant_b_data.media %}
                                  <img src="{{ media_item.preview_url }}" alt="{{ media_item.alt | default: variant_b_data.title | default: shg_preview_product.title }}" />
                                {% endfor %}
                              {% else %}
                                <span class="shg-preview__variant-image-placeholder" aria-hidden="true"></span>
                              {% endif %}
                            </div>
                            {% if shg_variant_b_has_description %}
                              <div class="shg-preview__variant-description" data-height-key="opt-{{ optimization_config.id }}-product-{{ shg_preview_product.id }}-variant-b">
                                <iframe
                                  title="{{ shg_preview_product.title | escape }} variant B description"
                                  class="shg-preview__variant-description-iframe"
                                  sandbox="{{ shg_description_iframe_sandbox }}"
                                  allow="autoplay; encrypted-media; fullscreen; picture-in-picture"
                                  srcdoc="{{ shg_variant_b_description_srcdoc | escape }}"
                                ></iframe>
                              </div>
                            {% endif %}
                          {% endif %}
                          <div class="shg-preview__product-variant-title">Product variants:</div>
                          <div class="shg-preview__variant-options">
                            {% for variant in shg_preview_product.variants %}
                              {% assign variant_test = variant.metafields[shogun_app_namespace]['product-variant-pricing'].value | find: 'shogun_variant_id', shg_second_variant_id %}
                              {% if variant_test %}
                                {%- assign shg_converted_price = variant_test.price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                                {%- assign shg_converted_compare_at = variant_test.compare_at_price | times: shg_currency_ratio | round: 2 | times: 100 -%}
                                <div class="shg-preview__variant-option">
                                  {{ variant_arrow_symbol }}
                                  {% if shg_is_product_details %}
                                    {% if variant_test.media and variant_test.media.preview_url %}
                                      <img class="shg-preview__variant-option-image" src="{{ variant_test.media.preview_url }}" alt="{{ variant_test.media.alt | default: variant.title }}" />
                                    {% else %}
                                      {% assign variant_b_fallback_media = variant_b_data.media | first %}
                                      {% if variant_b_fallback_media and variant_b_fallback_media.preview_url %}
                                        <img class="shg-preview__variant-option-image" src="{{ variant_b_fallback_media.preview_url }}" alt="{{ variant_b_fallback_media.alt | default: variant.title }}" />
                                      {% else %}
                                        <span class="shg-preview__variant-option-image-placeholder" aria-hidden="true"></span>
                                      {% endif %}
                                    {% endif %}
                                  {% endif %}
                                  <span class="shg-preview__variant-option-text">{{ variant.title | default: "Default" }}:</span>
                                  <span class="shg-preview__variant-option-price">
                                    {{ shg_converted_price | money }}
                                    {% if variant_test.compare_at_price != blank and variant_test.compare_at_price != "" and variant_test.compare_at_price != "0" and variant_test.compare_at_price != "0.00" %}
                                      <span class="shg-preview__compare-at-price">({{ shg_converted_compare_at | money }})</span>
                                    {% endif %}
                                  </span>
                                </div>
                              {% endif %}
                            {% endfor %}
                          </div>
                          {% if shg_is_product_details and variant_b_data.product_options and variant_b_data.product_options.size > 0 %}
                            <div class="shg-preview__variant-option-order-title">Variant option order:</div>
                            <div class="shg-preview__variant-option-order">
                              {% for option in variant_b_data.product_options %}
                                {% if option and option.values and option.values.size > 0 %}
                                  <div class="shg-preview__variant-option-order-row">
                                    <span class="shg-preview__variant-option-order-label">{{ option.name | default: 'Option' }}</span>
                                    <div class="shg-preview__variant-option-order-chips">
                                      {% for option_value in option.values %}
                                        <span class="shg-preview__variant-option-order-chip">{{ option_value.name | default: 'Value' }}</span>
                                      {% endfor %}
                                    </div>
                                  </div>
                                {% endif %}
                              {% endfor %}
                            </div>
                          {% endif %}
                        </div>
                      </div>
                    {% endif %}
                  {% endif %}
                </div>
              </div>
            {% endif %}
          {% endfor %}
        </div>
      </div>
    </template>

    <script>

      const COLLAPSED_STATE_KEY = 'shogun_price_test_sidebar_collapsed_state';
      const DESCRIPTION_HEIGHT_STATE_KEY = 'shogun_price_test_sidebar_description_heights';
      const DESCRIPTION_HEIGHT_SAVE_DELAY_MS = 120;
      const descriptionHeightSaveTimers = {};
      const pendingDescriptionHeights = {};
      let descriptionHeightResizeObserver = null;
      let descriptionHeightFlushHandlersBound = false;

      function saveCollapsedState(elementId, isCollapsed) {
        try {
          const state = JSON.parse(localStorage.getItem(COLLAPSED_STATE_KEY) || '{}');
          state[elementId] = isCollapsed;
          localStorage.setItem(COLLAPSED_STATE_KEY, JSON.stringify(state));
        } catch (e) {
          console.warn('Failed to save collapsed state:', e);
        }
      }

      function restoreCollapsedState() {
        try {
          const state = JSON.parse(localStorage.getItem(COLLAPSED_STATE_KEY) || '{}');

          Object.keys(state).forEach(elementId => {
            const element = document.getElementById(elementId);
            if (element) {
              const caret = element.previousElementSibling?.querySelector('.shg-preview__product-caret');

              if (state[elementId]) {
                element.classList.add('collapsed');
                if (caret) {
                  caret.classList.add('collapsed');
                }
              } else {
                element.classList.remove('collapsed');
                if (caret) {
                  caret.classList.remove('collapsed');
                }
              }
            }
          });
        } catch (e) {
          console.warn('Failed to restore collapsed state:', e);
        }
      }

      function getDescriptionHeightState() {
        try {
          const parsed = JSON.parse(localStorage.getItem(DESCRIPTION_HEIGHT_STATE_KEY) || '{}');
          if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) return {};
          return parsed;
        } catch (e) {
          console.warn('Failed to read description height state:', e);
          return {};
        }
      }

      function saveDescriptionHeightState(heightKey, heightPx) {
        if (!heightKey || !Number.isFinite(heightPx) || heightPx <= 0) return;

        try {
          const state = getDescriptionHeightState();
          state[heightKey] = Math.round(heightPx);
          localStorage.setItem(DESCRIPTION_HEIGHT_STATE_KEY, JSON.stringify(state));
        } catch (e) {
          console.warn('Failed to save description height state:', e);
        }
      }

      function queueDescriptionHeightStateSave(heightKey, heightPx) {
        if (!heightKey || !Number.isFinite(heightPx) || heightPx <= 0) return;

        pendingDescriptionHeights[heightKey] = heightPx;

        if (descriptionHeightSaveTimers[heightKey]) {
          clearTimeout(descriptionHeightSaveTimers[heightKey]);
        }

        descriptionHeightSaveTimers[heightKey] = setTimeout(() => {
          const pendingHeight = pendingDescriptionHeights[heightKey];
          saveDescriptionHeightState(heightKey, pendingHeight);
          delete pendingDescriptionHeights[heightKey];
          delete descriptionHeightSaveTimers[heightKey];
        }, DESCRIPTION_HEIGHT_SAVE_DELAY_MS);
      }

      function flushDescriptionHeightStateSaves() {
        Object.keys(pendingDescriptionHeights).forEach((heightKey) => {
          if (descriptionHeightSaveTimers[heightKey]) {
            clearTimeout(descriptionHeightSaveTimers[heightKey]);
            delete descriptionHeightSaveTimers[heightKey];
          }

          const pendingHeight = pendingDescriptionHeights[heightKey];
          saveDescriptionHeightState(heightKey, pendingHeight);
          delete pendingDescriptionHeights[heightKey];
        });
      }

      function bindDescriptionHeightFlushHandlers() {
        if (descriptionHeightFlushHandlersBound) return;
        descriptionHeightFlushHandlersBound = true;

        window.addEventListener('beforeunload', flushDescriptionHeightStateSaves);
        window.addEventListener('pagehide', flushDescriptionHeightStateSaves);
        document.addEventListener('visibilitychange', () => {
          if (document.visibilityState === 'hidden') {
            flushDescriptionHeightStateSaves();
          }
        });
      }

      function getDescriptionHeightResizeObserver() {
        if (typeof ResizeObserver !== 'function') return null;

        if (!descriptionHeightResizeObserver) {
          descriptionHeightResizeObserver = new ResizeObserver((entries) => {
            entries.forEach((entry) => {
              const target = entry.target;
              const heightKey = target && target.getAttribute ? target.getAttribute('data-height-key') : null;
              if (!heightKey) return;
              queueDescriptionHeightStateSave(heightKey, entry.contentRect.height);
            });
          });
        }

        return descriptionHeightResizeObserver;
      }

      function restoreDescriptionHeightState() {
        bindDescriptionHeightFlushHandlers();
        const state = getDescriptionHeightState();

        const descriptionElements = document.querySelectorAll('.shg-preview__variant-description[data-height-key]');
        const resizeObserver = getDescriptionHeightResizeObserver();
        descriptionElements.forEach((element) => {
          const heightKey = element.getAttribute('data-height-key');
          if (!heightKey) return;

          const savedHeight = Number(state[heightKey]);
          if (Number.isFinite(savedHeight) && savedHeight > 0) {
            element.style.height = `${savedHeight}px`;
          }

          if (resizeObserver) {
            if (element.getAttribute('data-height-observer-bound') === 'true') return;
            resizeObserver.observe(element);
            element.setAttribute('data-height-observer-bound', 'true');
          } else {
            if (element.getAttribute('data-height-fallback-bound') === 'true') return;
            element.addEventListener('mouseup', () => {
              const computedHeight = Number.parseFloat(getComputedStyle(element).height);
              saveDescriptionHeightState(heightKey, computedHeight);
            });
            element.addEventListener('touchend', () => {
              const computedHeight = Number.parseFloat(getComputedStyle(element).height);
              saveDescriptionHeightState(heightKey, computedHeight);
            });
            element.setAttribute('data-height-fallback-bound', 'true');
          }
        });
      }

      function toggleElement(elementId) {
        const children = document.getElementById(elementId);
        if (!children) return;
        const caret = children.previousElementSibling?.querySelector('.shg-preview__product-caret');
        if (!caret) return;

        if (children.classList.contains('collapsed')) {
          // Expand: remove collapsed class first, then rotate caret
          children.classList.remove('collapsed');
          caret.classList.remove('collapsed');
          saveCollapsedState(elementId, false);
        } else {
          // Collapse: add collapsed class, then rotate caret
          children.classList.add('collapsed');
          caret.classList.add('collapsed');
          saveCollapsedState(elementId, true);
        }
      }

      function toggleProduct(productId) {
        toggleElement(productId);
      }

      function contactSupport() {
        const subject = encodeURIComponent('A/B Testing Question');
        const body = encodeURIComponent('Hi Shogun Team,\n\n');
        const mailtoLink = `mailto:support-ab@getshogun.com?subject=${subject}&body=${body}`;
        window.open(mailtoLink, '_blank');
      }

      window.restoreCollapsedState = restoreCollapsedState;
      window.restoreDescriptionHeightState = restoreDescriptionHeightState;
      window.flushDescriptionHeightStateSaves = flushDescriptionHeightStateSaves;
    </script>
  {% endif %}
{%- endif -%}

{%- comment -%}
  =====================================================
  OPTIMIZER INITIALIZATION
  =====================================================
{%- endcomment -%}
<script type="text/javascript">
  ;(function() {
    const designMode = {{ request.design_mode | default: false }};
    if (designMode) {
      console.debug("shogun: Design mode is enabled, skipping optimizer initialization");
      return;
    }

    // an Error cannot be sent as-is: message and stack are non-enumerable, so JSON.stringify gives {}
    const describeThrown = (value) =>
      value instanceof Error ? { message: value.message, stack: value.stack } : String(value);

    const reportExclusion = (reason, details) => {
      try {
        if (typeof fetch !== 'function') return;

        fetch("https://shogun-abc-production.global.ssl.fastly.net/dispatcher/excluded", {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            name: 'excluded',
            shop_id: "2d5a250f-daa8-446c-923c-67a88dc774be",
            reason: reason,
            details: details || {}
          }),
          keepalive: true,
        }).catch(() => {});
      } catch (_error) {
        // reporting a bail must never be a thing that breaks a merchant's page
      }
    };

    // If the referrer url is the Shopify admin url or other known merchant domains, then set a local storage shg_is_merchant flag to true
    // This is used to remove the preview bar from the page for shoppers
    //
    // Reading localStorage throws outright where the origin is denied storage, and this only decides whether a
    // merchant sees the preview bar. Letting it escape would take the dispatcher down with it and leave the visitor
    // reporting nothing at all.
    try {
      const referrer = document.referrer
      const merchantDomains = ['admin.shopify.com', 'ab.shgapi.com', 'abc.shogun.dev']
      if (!localStorage.getItem('_shg_is_merchant') && merchantDomains.some(domain => referrer.includes(domain))) {
        console.debug('shogun: Setting shg_is_merchant to true')
        localStorage.setItem('_shg_is_merchant', 'true')
      }
    } catch (error) {
      console.debug('shogun: could not read the merchant flag, continuing without it', error)
    }

    // Check for optimization disable flags
    const urlParams = new URLSearchParams(window.location.search);
    const optimizationDisabled = urlParams.get('shg') === "false" || window.location.hostname.includes('shopifypreview');

    if (optimizationDisabled) {
      console.debug("shogun: Optimization is disabled, skipping optimizer initialization");
      return;
    }

    // Load shop metafield configuration
    {% if shop.metafields.shgabc.optimizations %}
    const shopMetafieldConfig = {{ shop.metafields.shgabc.optimizations }};
    {% else %}
    const shopMetafieldConfig = null;
    {% endif %}
    if (!shopMetafieldConfig) {
      console.debug("shogun: Shop metafield configuration for optimizations is missing. Skipping optimizer initialization.");
      reportExclusion('config_unavailable');
      return;
    }

    // A lapsed metafield yields no optimizations, which on its own is indistinguishable from a shop running none.
    const configExpired = !(shopMetafieldConfig.expires_at > Date.now());
    const optimizationsData = configExpired ? [] : (shopMetafieldConfig.optimizations || []);

    // These ID should match. It is possible for them to be different if a theme is copied from a different shop.
    const intendedShopifyId = "87335469381"; // The hard coded shopify ID when this snippet was pushed to the theme.
    const currentShopifyId = "{{ shop.id }}"; // The current shopify ID that this theme is rendered in.
    if (intendedShopifyId !== currentShopifyId) {
      console.debug(`shogun: Shopify ID mismatch: intendedShopifyId=${intendedShopifyId} currentShopifyId=${currentShopifyId}. This may indicate the theme was copied from another shop and not properly set up. Skipping optimizer initialization to avoid potential issues.`);
      // Attributed to the shop the snippet was written for, the only internal id it carries; both Shopify ids ride
      // along so the shop it is actually running on can be resolved.
      reportExclusion('shop_mismatch', { intended_shopify_id: intendedShopifyId, current_shopify_id: currentShopifyId });
      return;
    } else {
      console.debug(`shogun: Shopify ID verified: ${intendedShopifyId}`);
    }

    
      if (typeof ShogunDispatcher === 'undefined') {
        console.error("shogun: Dispatcher is not defined. Please ensure the shogun optimizer script is properly loaded.");
        reportExclusion('dispatcher_unavailable', { optimizer_version: 2 });
        return;
      }

      const dispatcherConfig = {
        shop: {
          shopId: "2d5a250f-daa8-446c-923c-67a88dc774be",
          shopDomain: "a5a19c-47.myshopify.com",
          analyticsUrl: "https://shogun-abc-production.global.ssl.fastly.net",
          geoLocationApi: "https://ipinfo.io/json?token=f2ae3a557d807b",
          storefrontAccessToken: "7cc75c6cb8a339472c373064a60723ea",
          defaultThemeId: shopMetafieldConfig.default_theme_id ? shopMetafieldConfig.default_theme_id.toString() : null,
          storewideEventCollection: true,
        },
        page: {
          pageId: "{{ content.id }}",
          pageType: "{% assign shg_page_types = 'search,cart,password,gift_card' | split: ',' %}{% if template.directory == 'customers' %}customers{% elsif template.name == 'list-collections' %}list-collections{% elsif article %}article{% elsif blog %}blog{% elsif page %}page{% elsif product %}product{% elsif collection %}collection{% elsif metaobject %}metaobject{% elsif template.name == 'index' %}homepage{% elsif template.name == '404' %}not_found{% elsif shg_page_types contains template.name %}{{ template.name }}{% else %}other{% endif %}",
          templateName: "{{ template.name }}",
          templateSuffix: "{{ template.suffix }}",
          templateDirectory: "{{ template.directory }}",
          themeId: "204532449605",
          languageRootUrl: "{{ localization.language.root_url }}",
          path: window.location.pathname,
          currentCartVariantId: {% if cart.attributes.shogun_variant_id %}"{{ cart.attributes.shogun_variant_id }}"{% else %}null{% endif %},
        },
        session: {
          sessionId: urlParams.get('shgSessionId'),
          customerId: "{{ customer.id }}",
          isB2B: {{ customer.b2b? | default: false }},
        },
        assignmentStrategy: shopMetafieldConfig.method,
        publishedOptimizations: optimizationsData,
        configExpired: configExpired
      }

      console.debug("shogun: Initializing ShogunOptimizer v2");
      try {
        const dispatcher = new ShogunDispatcher(dispatcherConfig);
        dispatcher.run();
      } catch (error) {
        console.error("shogun: Dispatcher failed to start.", error);
        reportExclusion('dispatcher_unavailable', { optimizer_version: 2, error: describeThrown(error) });
      }
    
  })();

</script>
